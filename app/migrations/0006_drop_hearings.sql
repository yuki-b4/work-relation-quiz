-- 商談前ヒアリングの廃止（2026-09-14）。
--
-- 読み解きガイド終章の2欄（いま悩んでいること／解消後の毎日）は廃止し、
-- 聞くのは体験セッションの場にした（集客戦略マップ.md §4.2 の1段目）。
-- 入力の口が無くなったので、置き場所も消す。
--
-- **消えるもの：移行データを含む、これまでの商談前ヒアリングの記録すべて。**
-- 戻せないので、残したい内容があるなら先に
-- `/admin/export/responses.csv` か `npm run db:backup` で控えを取ること。
--
-- 0001〜0005 は編集せず、変更はこのファイルで足す（適用済みのファイルを直しても流れ直さない）。

drop table if exists hearings;
