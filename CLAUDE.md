# CLAUDE.md

## プロジェクト概要

人間関係タイプ診断アプリ。9問の質問から、「普段のあなた（自然体のあなた）」の関わり方を8タイプで診断する。

- **立ち位置**：診断は特定の場面（職場など）を前提にせず、自然体の対人スタイルそのものを映す。職場・プライベートに共通で使える。文脈（職場向け／大切な人向け）は入口（LP・結果画面）で出し分ける。
- **提供価値（差別化ポイント）**：普段のあなたを知り、それを踏まえたコンサル（読み解き・セッション）を行うことで、職場やプライベートの人間関係の居心地が良くなるきっかけをつくる。診断は「場面ごとの振る舞い」ではなく「自然体のあなた」を測るのが肝。
- **役割**：診断は無料の共通入口かつ共通言語（＝関係を語る語彙）。収益は診断そのものでなく、その先の人的サービス層（ワークショップ・セッション・ガイド）で立てる。詳細な事業設計・ファネルは `ファネル図.md` と参照先スプレッドシートを見る。
- 設問は職場前提の言い回し（会議・タスク等）をあえて排し、場面に依存しない自然体の傾向を測る方針とする。

- `prototype.html` — 検証用のプロトタイプ（スタンドアロンのHTML/CSS/JS）。`?ref=` から4セグメントを判定して出し分ける（`general`＝refなし／`referral`＝紹介者コード／`leader-lp`＝`corp-lp`・`wedding-lp`・`coffee-lp`／`member`＝`team_` 始まり。`?seg=` で上書き可）。検証アンケート5問は `?mode=feedback` を明示したときだけ表示。②紹介では結果画面から**アプリ内の読み解きガイド（全4章・本の体裁）**へ進み、終章で商談前ヒアリング2項目（すべて任意）と体験セッション申込（外部フォーム・`SESSION_FORM_URL`）に着地する。メールアドレスの登録は取らない（クリックだけで読み進められる）。①一般・③④にはガイドCTAを出さない
- `docs/` — **GitHub Pages の公開元**（2026-09-07に直下から移した）。中身は旧URLから新ドメインへのリダイレクト3枚（`index.html`・`prototype.html` → `https://natur-indicator.com/`、`all-types.html` → `/types`。いずれもクエリとハッシュを引き継ぐ）と、法人LP3枚。**リポジトリ直下の `prototype.html` と `all-types.html` はアプリの文面の正なので、公開元を移すことで触らずに旧URLだけ差し替えている。** プレビューURLは `raw.githubusercontent.com` を見ているので影響しない
- `docs/lp-corporate.html` — 法人向けLP（Tier1業種の決裁者向け）。無料診断への導線（別タブ・`https://natur-indicator.com/?ref=corp-lp`。**アプリでは検証アンケートを廃止したので `mode=feedback` は付けない**）と商談フォーム（メール＋課題チェック＋自由記述）を持つ。フォームは Apps Script の `action:corpLead` で「法人リード」シートに記録（要再デプロイ。詳細は `apps-script/README.md`）
- `docs/lp-wedding.html` / `docs/lp-coffee.html` — 法人LPの業種特化版（結婚式場版・珈琲屋カフェ版。初期フォーカス業種）。`docs/lp-corporate.html` を親版とし、業種の言い当てモジュール（名乗り・悩み・放置コスト・ユースケース・フォーム課題・ref既定値 `wedding-lp`／`coffee-lp`）のみ差し替え。珈琲屋版は創業者の元珈琲屋オーナーというルーツを信頼要素（founder story）として組み込む。構成・コピーを変える際は `法人LP情報設計.json` の `variants` を先に更新して同期する。親版の共通部分を変更したら特化版2枚にも同じ変更を反映する
- `法人LP情報設計.json` — 法人LPの情報設計（構成・コピー・フォーム仕様のソース）。LPの構成やコピーを変える際はここを先に更新して同期する
- `apps-script/` — 診断結果・検証フィードバック・紹介者リード（メール/自由記述）をGoogleスプレッドシート（回答ログ）に記録する Google Apps Script Web App とセットアップ手順。`CreateSessionForm.gs` は体験セッション申込フォームの生成スクリプト（1回だけ実行する）。`UpdateRoadmapSheets.gs` は事業ロードマップブックの `0_全体サマリー`・`法人_12ヶ月ロードマップ`・`90日WBS_アクション` を書き換えるスクリプト（ロードマップブックにバインドして実行。旧シートは `_旧YYYYMMDD` に退避され消えない）。ロードマップの内容を変えるときはこのスクリプトの `moneyRows`／`rows`／`tasks` を直して再実行する
- `体験セッション申込フォーム.md` — 読み解きガイド終章から開く申込フォーム（Googleフォーム）の仕様と文言の正。フォームを変えるときはここを先に更新する
- `guide-3day-spec.md` — 「3日間の読み解きガイド」の仕様（v2：自然体・関係非依存の個人文脈）。共通ガワ（§8）とタイプ別固定文24本（`ガイド文面24本.md`）は**現在アプリ内ガイド（`prototype.html` の全4章）の文面ソース**。**メール配信（§9・`apps-script/Guide.gs`）は2026-07-27に停止**し、稼働セグメントはない（①一般はライブ経由、②紹介はアプリ内完結）。仕様と実装はそのまま温存する。着地は体験セッション → 個別コーチングセッション。
- `ガイド文面24本.md` — タイプ別本文24本（8タイプ×3日）の正。アプリ内ガイドは第一章・第二章で使い、Day3本文（関係のクセ）は現在未使用。文言を変えるときはここを直し、`prototype.html` の `GUIDE_BODY` と `apps-script/GuideContent.gs` に反映する。
- `法人中心価値とファネル.md` — セグメント③現場責任者／④メンバーの**中心価値・価格・商品定義・ペルソナ・ファネル**の正。法人の中心価値（約束）は「辞めない店をつくる（定着）」で、それを動かすレバーは「責任者の関わり方」。**主語は役職名でなく構造で決まる**（総称は「現場責任者」。店長・院長・施設長・所長・支配人・室長は業種別の呼び名で、設計では総称、売るときは具体名を使う）。最高単価は現場責任者への継続伴走1on1（＝定着の伴走・年契約）。定着は「離職率が◯%下がる」でなく「辞める理由そのものを減らす」の粒度で約束する。解決する悩みは3層に1つずつ（約束＝静かに辞める／商談＝自分の何が人を遠ざけるか／商品＝相手ごとの基準がない）。セグメントは観測可能な構造条件で切る（単一拠点／5〜30名／中間管理職なし＋トリガー）。**価値観は境界に置かない**（観測できないため。商談で作るものとして扱う）。業種フォーカスはチャネル扱いで、**Tierは離職率でなく到達しやすさで並べる**（小規模ほど離職率が高いので規模条件が既にスクリーニングになっている）。価格は**相場基準をやめて改定済み**（定着の伴走が規模帯別で月10万／15万／20万円、チームWS単独は年50万円。入口の単発セッションは廃止し、初月のリスク引き受けに置き換え）。**比較対象を「コンサルの相場」から「人にかけている金」（パート1人分の人件費・年間の採用予算）へ変えるのが値付けの肝**で、コンサルと名乗った時点で相場の棚に入る。この価格に値するための要件6項目は同ファイル §2.7.5、それを落とした商品定義は §2.8（週次チェックイン・月次レビュー90分・オンデマンド48時間・半期レポート更新の4接点と、見取り図・トリセツ・関わりの記録の3資産）。**チームレポートのM3自動化が年5,000万円の前提条件**。④には売らず、自己理解（トリセツ）だけを返す。法人まわりの方針を変えるときはここを先に更新し、`ファネル図.md`・`法人ポジショニング.md`・`提案資料1枚.md` に反映する
- `調査_類似サービスの売上実例.md` — 「法人が個人（経営者・店長）を育てるために払う」型のサービスが実際に年5,000万円規模へ伸びたのかの調査記録（識学・EOS・グローイング・アカデミー・日本の価格相場）。**モデルは成立するが、実証済みの例は全て単価が一桁上**。値付け・ターゲット規模を議論するときはここを参照する
- `0to1マーケティング戦略.md` — 0→1フェーズ（③現場責任者の最初の10社獲得）の実行戦略（2026-08-07策定）。主エンジンは紹介でなく**トリガー観測型の直接接触**（求人を出し続けている事業所のリスト化×訪問・手紙・登壇。管理変数は成約数でなく週の接触件数）。紹介はサブ（後段）でパイプラインに数えない。「型の無料提供」は知識でなく**測定（チームの実データ）と実行体験**を配る形に再定義し、無料ラダーの終端に「1週間後フォロー（一手の確認）」を新設。無料と有料の境界線は「知ることと一回きりの測定は無料、続く測定・やらせる装置・できているの判定は有料」。買い手の「AIで十分」への標準反論を含む。自分ごと化は相手の固有情報で起こし、初回接触では売らない（接触反応の3分類で「営業と思われたか」を検知する）。③の獲得まわりの方針を変えるときはここを先に更新する
- `集客戦略マップ.md` — **個人（toC）ルートの集客戦略の正**（2026-09-09策定）。課題（ガイド → 体験セッション申込の転換率、商談の温度）を3つの断層に分解し、**①価値の翻訳が無い ②問題の宣言が導線の最後にしか、しかも任意でしか起きない ③本気度をつくる装置が無い**と特定する。打ち手は「問題別コンテンツを増やす」でなく「**宣言 → 見立て → 一手 → 判定**」の4段への組み替えで、**教育スキームは新しく作らない**（現行ガイドが既に「図星 → 道具 → 知ってるだけでは変わらない → 一人では分からない」の4段を立てているので、題材を「あなたのタイプ」から「あなたとあの人」に差し替えて複製する。**ガイドは「マインド」「価値観」と一度も書いていない**ので、そのまま繋がる）。分岐させるのは読み物でなく**本人の宣言（DBに記録する）**。最初に作るドメインは職場で、その約束は「職場を変える」でなく「**自分だけがすり減る関係性を終わりにして、自然体でいられる関係性を始める。**」（文言は2026-09-10確定。やめる先＝自然体を足したことで診断とガイドの語彙につながる。場面別ページでは「あの人」を補って具体に落とす）（個人がボトムアップで組織を変える動機は弱いという既存の結論を覆さないため）。無料と有料の境界線は法人で決めた線をそのまま個人にも適用する。**転職エージェントは送客先でなく顧客にする**（送客マージンは採らない。人材紹介会社を法人セグメントの1チャネルとして扱い、既存商品＝定着の伴走／チームWSをそのまま売る。新規に作るものはゼロ。境界条件を満たし、自社のCA募集を出し続けているのでトリガーの観測が最も容易。刺さる理由は早期離職の返金規定で、背後に顧客企業が数十社ある。**成果報酬は一切受け取らない**。`法人中心価値とファネル.md` §3.1.6.2 にもチャネルとして登録済み）。**個人のバックエンド価格は3ヶ月55万円／6ヶ月66万円・一括（既定は6ヶ月）**で、一括なので契約した月がそのまま月商になる。月商目標に対する逆算（10月＝2本、11〜12月＝3本／月）・必要な流入（11月以降は月300件前後の診断完了）・広告の許容CPA（470〜850円。申込率が上がるとそのまま上限も上がる）・計測のSQL・90日の実行順を持つ。個人ルートの方針を変えるときはここを先に更新し、`ファネル図.md` に反映する
- `個人商品定義.md` — **個人（toC）バックエンド商品「ナチュール読み解きプログラム」の正**（2026-09-09策定）。**商品名にコーチング・コンサルティング・カウンセリングを使わない**（名乗った時点でその相場の棚に入る）。無料側の語彙（読み解きガイド）と揃えることで、ナチュール診断 → 読み解きガイド → 読み解きプログラムと階段になる。**コース名は持たない**（1つのプログラムに期間が2つ。ベーシック／スペシャルは廃止）。`法人中心価値とファネル.md` §2.8 の個人版にあたる。**約束（何を売るか）とレバー（実際に動かすもの）を分ける**のが設計の核で、`法人中心価値とファネル.md` §1.1 と対になる（**約束＝「自分だけがすり減る関係性を終わりにして、自然体でいられる関係性を始める。」／レバー＝その人のマインド／最高価値の位置＝己成合宿**）。**マインドを中心価値に置き換えない**（置き換えると診断が共通言語でなくなり、コーチング・自己啓発の棚に入り、「自然体な自分になる」と同じ抽象度に戻って出発点の課題が再発する）。骨格は「**名指し → 一手 → 判定**」の3ステップで、**タイプを入口の索引で終わらせず、自分のタイプ×相手のタイプの力学（`相性ルール.md` の6カテゴリ）を最後まで使う**。**「原因は価値観にある」と先に宣言しない**（診断とガイドの「裏モードは欠陥でなく伸び代」という教育と反転するため）。変えるのは価値観でなく**関わり方の一手**で、価値観に降りるのは一手が出ない・効かないときだけ。**己成合宿（マインドを変える2days・オンライン。日程は個別調整）は期間の中盤**に置く（一手を2周させた後）。接点は3つ（**隔週のセッション45分**・己成合宿・いつでも相談48時間以内）、資産は2つ（関係の見取り図・トリセツ更新版）。**週次の記録は接点から外し**、隔週セッションの冒頭で一緒に書き出す形に畳んだ（毎週の報告はこちらの都合であって相手の便益でなく、離脱を生むため。記録はこちら側の計器として内部に持ち、渡すのは記録から作った見取り図とトリセツ）。**成果は本人の内面でなく関係の側で測る**（評価でなく出来事＝相手の言動を記録し、期首と期末で比べる。**測るために本人に何も課さず、セッションの場とメモから取る**）。価格は3ヶ月55万円／6ヶ月66万円・一括（既定は6ヶ月。違いは期間でなく「**3ヶ月＝一人との関係性を攻略する／6ヶ月＝複数人との関係性を攻略する**」で説明する。**攻略の対象は関係であって人ではない**（「相手を攻略する」とは書かない。恋愛・結婚ドメインではとくに注意））で、**回数と時間で売らず、期間の終わりに残るもので提示する**。**豊かさを極める8要素は廃止**、目標設定・自己実現は約束から外した。レバーの置き方は**一手の実行率（できなかった率）で検証する**（高ければマインドがボトルネックで合宿の比重を上げる、低ければ現状維持。どちらでも骨格は作り直さずに済む）。商品の方針を変えるときはここを先に更新し、`集客戦略マップ.md`・`ファネル図.md` と営業資料（Canva）に反映する
- `文脈出し分け仕様.md` — 個人／法人で出し分ける入口・立場・CTAのコピー素材。結果カードの核は共通のまま。LP制作フェーズ（M2〜M3）で `?ctx=` 実装時に参照。
- `アプリ化要件定義.md` — GitHub Pages の静的ページからアプリケーション（DB＋Admin画面）へ移行するための要件定義。**アプリ化まわりの要件の正**。データのDB化・Admin画面・結果のワンタイム表示とデバイス束縛・X共有・SEOの7要件（F1〜F7）と、確認事項・リリース計画を持つ。アプリ化の方針を変えるときはここを先に更新する。
- `アプリ化実装状況.md` — アプリ化の**実装がどこまで進んでいるか**の記録。本番稼働中で、公開後に出てきた運用の穴（D-1〜D-7）まで整理してある。作業の約束（文面とCSSは機械抽出・書き写さない）、ハマった箇所、次にやることを持つ。セッションをまたぐときは `アプリ化要件定義.md` とこれを最初に読む
- `app/` — アプリ本体（Cloudflare Workers + D1 + Hono）。`prototype.html` を置き換える。**`https://natur-indicator.com` で本番稼働中**（Phase 2 完了、Phase 3 は成果物を出し切り。Phase 4 は保留）。診断→結果→ガイド→申込が通しで動き、`/admin` から運用できる。診断の文面とCSSは `prototype.html` から機械抽出しているので、**文面を変えるときは `prototype.html` を直して `npm run content`**（`app/src/content/*.ts` は生成物なので直接編集しない）。セットアップ・Admin・データ移行の手順は `app/README.md`。実装は `アプリ化要件定義.md` に従う。**アプリでは `?ref=` によるセグメント出し分けと検証アンケートを廃止し、ガイドCTAは全員に出す**（確認事項9＝b。`prototype.html` の出し分けとは意図的に違う）
- `公開ページ文面.md` — `/about`・`/faq`・`/contact` に載せる文面の正。`## /パス` で1ページ、直後の ```meta` に `title` と `description`、`###` が節の見出し（`/faq` では質問そのものになり FAQPage の構造化データに入る）。**文言を変えたら `cd app && npm run content`**
- `プライバシーポリシー.md` / `利用規約.md` — `/privacy`・`/terms` に載せる文面の正。事業者情報は記入済み（Mikata／齋藤祐希）だが、**公開前に専門家の確認が要る**。文言を変えるときはここを先に更新し、`cd app && npm run content` で反映する
- `調査_ナチュール同名調査.md` — 「ナチュール」の同名・商標・ドメインの調査記録（Phase 0）。複合語「ナチュール診断」は空いている見込みだが、単体の「ナチュール」はワイン（ヴァン・ナチュール）が支配的なので単体では狙わない。商標（J-PlatPat）とドメインの空き確認は**未実施**

## プロジェクト開始日

- **プロジェクト開始日**：2026年6月12日（Day 1）
- このプロジェクト開始から何日経過したかを聞かれた際はこの日付を基準日とする

## 表記ルール

- **診断コピー・本文で「——」（ダッシュ）を使用禁止**。ためらい・転換・言い換えは、句点「。」読点「、」や「：」「（）」などで自然につなぐこと。単独の「—」も本文では使わない。

## プレビューURLの提示ルール

- フロントエンド（`prototype.html`）の変更をブランチにプッシュしたら、ユーザーが指示しなくても**毎回**ブランチのプレビューURLを提示する。
- 形式：`https://htmlpreview.github.io/?https://raw.githubusercontent.com/yuki-b4/work-relation-quiz/refs/heads/<ブランチ名>/prototype.html`
- URLはブランチ＋ファイルで**固定**（毎回同じ）。最新の内容はプッシュ後、`raw` のCDNキャッシュ（数分）で反映される。
- バックエンドのみ（`apps-script/` 等）の変更にはプレビューを出さず、再デプロイが必要な旨を伝える。

## ローカル実行を指示するときのルール

- ユーザーに手元のPCでコマンド（デプロイ・マイグレーション・秘密値の設定など）を実行してもらう場合は、
  **対象ブランチへのチェックアウトと `pull` までのコマンドを必ず先に添える**。省略しない。
- 理由：手元が古いブランチのままだと、**そのコマンド自体が存在しない**（`package.json` の
  スクリプトや設定が入っていない）か、**古い内容をデプロイしてしまう**。
- 形式（対象ブランチが `<ブランチ名>` のとき）：

  ```
  cd <リポジトリのパス>
  git fetch origin
  git checkout <ブランチ名>          # 初回は git checkout -b <ブランチ名> origin/<ブランチ名>
  git pull origin <ブランチ名>
  npm install                        # package.json が変わっているときだけ
  ```

- そのうえで、本題のコマンドを続けて示す。
- ローカルでファイルを編集させる指示（設定値の貼り付けなど）を含む場合は、**そのあとの
  commit と push まで**セットで示す。書いたまま push し忘れると、次のセッションで消える。

## データソース

このプロジェクトのソース、および今後の開発・Funnel 1 の90日ロードマップ・12ヶ月計画は、以下のGoogleスプレッドシートを唯一の参照先とする（旧 `ROADMAP.md`・旧スプレッドシートは廃止）。

- https://docs.google.com/spreadsheets/d/1eb-P739JJs7qi1c9Uzy_WpZhy0gsCag382YzohFOBEY/edit

### ルール：スプレッドシート参照時のシート指定

- ユーザーがこのスプレッドシートを参照する際は、**必ずシート指定（複数可）があるはず**である。
- もしユーザーの指示に**シート指定がない場合は、作業を進める前に必ず指摘し、どのシートを参照すべきか確認する**こと。
- シート指定がないまま推測でシートを選んで作業を進めてはいけない。
