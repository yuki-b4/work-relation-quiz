# アプリ化 実装状況（Phase 2 完了時点）

最終更新：2026-09-05 ／ ブランチ `claude/nature-diagnosis-phase-2-0vi1fm`

要件の正は **`アプリ化要件定義.md`**（v6）。この文書は「どこまでできていて、次に何をするか」の記録。
セッションをまたいで作業を引き継ぐときは、まずこの2つを読む。

## 1. 現在地

| Phase | 内容 | 状態 |
|:--|:--|:--|
| 0 | 準備（ドメイン・商標調査・環境構築・ポリシー草案） | **完了** |
| 1 | 基盤と回答画面（診断から申込まで） | **完了** |
| 2 | Admin とデータ移行 | **完了（移行スクリプトまで。実データの投入は未実施）** |
| 3 | SEO本格化（FAQ・about・Search Console） | 一部前倒しで実装済み |
| 4 | 拡張（法人LP移設・A/Bテスト基盤） | 未着手 |

**診断を受けて、結果を見て、ガイドを読んで、体験セッションに申し込むところまで通しで動く。**
**その全部が Admin の1画面で見え、対応状況とメモを付けられる。** 公開ページ（トップ・全8タイプ・
タイプ個別8枚）もインデックスできる状態になっている。

まだ**本番へはデプロイしていない**（`npm run deploy` は未実行。本番D1へのマイグレーションも未実行）。
**実データの移行も未実施**（スクリプトと試験はある。CSVを書き出して流すのは手元の作業）。

## 2. 決まっていること

確認事項11件はすべて確定済み（`アプリ化要件定義.md` 第10章）。実装に効くものを再掲する。

| # | 決定 |
|:--|:--|
| 1 | X共有の文面は `私は「{タイプ名}」でした！…` ＋改行。`<br>` は使わない |
| 2 | 結果画面はリロードでは消えない |
| 3 | 申込フォームはアプリ内に内製化（Googleフォーム廃止） |
| 4 | 申込フォームだけワンタイムの外。**タイプ別の共通ページ**で、到達ID `?v=` で回答に紐づける |
| 5 | ドメインは `natur-indicator.com`（Cloudflare Registrar で取得済み） |
| 6 | 全8タイプはアプリに取り込む。法人LPは Phase 4 |
| 7 | 保存期間3年。**自動削除バッチは作らない** |
| 8 | Admin は1名運用。`role` は持たない |
| 9 | セグメント出し分けと検証アンケートは廃止。`?ref=` は記録だけ残す |
| 10 | 集計ダッシュボードは作らない |
| 11 | AI学習クローラだけ弾く。AI検索・エージェントは通す |

Phase 2 で要件から変えたのは1点だけ。**パスワードのハッシュを bcrypt / argon2 から
PBKDF2-HMAC-SHA256・600,000回（WebCrypto）へ**変えた（Workers にネイティブ実装が無いため。
OWASP が代替として挙げている方式）。`アプリ化要件定義.md` F2-1 に理由ごと反映済み（v6）。

## 3. 環境

| 項目 | 値 |
|:--|:--|
| ドメイン | `natur-indicator.com`（取得済み・未接続） |
| ホスティング | Cloudflare Workers ＋ D1 |
| D1 database_id | `414c5d47-267d-4aa4-ba62-c8dafee15151`（`app/wrangler.toml` に設定済み） |
| wrangler | v4 系 |
| フレームワーク | Hono |
| 秘密値 | `IP_HASH_SALT`／`ADMIN_BOOTSTRAP_*`／ログイン通知の設定。ローカルは `.dev.vars`、本番は `wrangler secret put` |

**未実施**：`wrangler deploy`、本番D1への `npm run db:migrate:remote`、Cloudflareダッシュボードでの
AIクローラー設定（F8）、Search Console 登録、**ログイン通知の設定**（未設定だと Admin に警告が出る）。

## 4. できているもの

### 画面（回答者向け）

| URL | 内容 | index |
|:--|:--|:--:|
| `/` | トップ＋フレーム＋設問24問（1枚の文書で切り替え） | ○ |
| `/result` | 結果カード（シェル＋3点一致で描画） | × |
| `/result/closed` | 「すでに閉じられています」の案内（410） | × |
| `/guide` | 読み解きガイド全4章 | × |
| `/apply/{typeCode}` | 体験セッション申込（8ページ・認可なし） | × |
| `/types` | 全8タイプ一覧＋3軸の説明 | ○ |
| `/types/{code}` | タイプ個別8ページ | ○ |
| `/about` | ナチュール診断とは（何を測り、何を測らないか・つくっている人） | ○ |
| `/faq` | よくある質問14件（FAQPage の構造化データ付き） | ○ |
| `/contact` | お問い合わせ窓口と事業者情報 | ○ |
| `/privacy` `/terms` | プライバシーポリシー・利用規約 | ○ |
| `/robots.txt` `/sitemap.xml` | | |
| 301 | `/quiz` `/prototype.html` `/index.html` → `/`、`/all-types.html` → `/types` | |

### 画面（Admin。すべて認証必須・noindex）

| URL | 内容 |
|:--|:--|
| `/admin/login` `/admin/logout` `/admin/bootstrap` | ログインと初期アカウント作成 |
| `/admin/responses` | 回答一覧。絞り込み7種・並べ替え3種・50件ページング |
| `/admin/responses/:id` | 回答詳細（8ブロック）。運用欄の編集、論理削除と物理削除 |
| `/admin/sessions` `/admin/sessions/:id` | 申込一覧と詳細。手動紐づけ、到達IDの重複警告 |
| `/admin/corp-leads` | 法人リード。メールは既定で伏せ、押したときだけ全表示 |
| `/admin/referrers` | 紹介者マスタ。コード発行・紹介リンク・実績（回答／ガイド到達／申込） |
| `/admin/export` ＋ `/admin/export/*.csv` | CSV出力4種（UTF-8 BOM付き） |

### API

`POST /api/responses`（回答の保存・結果セッション発行）／`/api/result/view`／`/api/result/close`／
`/api/guide/view`／`/api/guide/progress`／`/api/hearing`／`/api/apply-visits`／
`/api/session-applications`／`/api/share`／`GET /api/health`

`/api/corp-leads` はまだ 501。法人LPの移設が Phase 4 のため（移行分は `corp_leads` に入る）。

### DB

`migrations/0001_init.sql`（12テーブル）＋ `0002_client_request_id.sql` ＋ `0003_admin.sql`
（CSRFトークン列・索引）。ローカルD1へ適用済み。**本番D1へは未適用**。

### 移行（第7章）

`app/tools/migrate-sheets.mjs`。4つのCSVを読んでSQLを書き出す。`--check` で照合レポートだけ出せる。
使い方は `app/README.md` の「データ移行」。

### 試験

```
npm run check         # content再生成 → parity → session → markup → auth → migrate → 型（サーバ不要）
npm run dev           # 別ターミナルで起動してから↓
npm run test:e2e      # HTTP：回答送信10項目 ＋ ワンタイム結果15項目
npm run test:browser  # ブラウザ：SEO → 診断の通し → ガイド → 申込とX共有 → Admin
```

- **採点は prototype.html の実装と5,516通りで一致**（9問の二択は512通り総当たり）
- 結果カードのクラス名・id が prototype.html と一致
- 別タブでは結果が開けないこと、公開ページに結果の中身が漏れていないことを機械で固定
- Admin：認証・ロック・マスク・CSV・設問文の引き当てを103件、移行の変換を62件で固定
- `test:admin` は**紐づけ候補になる回答を毎回自分で作る**ので、何度でも回せる

## 5. 作業の約束（守らないと静かに壊れる）

1. **文面とCSSは書き写さない。** `npm run content` が、文面の正から `src/content/*.ts` を
   機械的に作る。生成物なので直接編集しない。文面を変えるときは正のほうを直して再生成する。

   | 文面の正 | 出る場所 |
   |:--|:--|
   | `prototype.html`（＋`ガイド文面24本.md`） | 設問・結果カード・ガイド・CSS |
   | `公開ページ文面.md` | `/about` `/faq` `/contact` |
   | `プライバシーポリシー.md` `利用規約.md` | `/privacy` `/terms` |
   （Admin の CSS だけは例外。prototype.html に無い画面なので手で書いている。ただし
   **色は APP_CSS の `:root` を切り出して使う**ので、診断側の色を変えれば追従する）
2. **採点に触ったら `npm run parity` を通す。** ここが崩れると F3-1 が静かに壊れる
3. **結果カードの見た目に触ったら `npm run test:markup` を通す。** クラス名を1つ間違えると
   その部分だけ無スタイルになり、目視では気づきにくい
4. **push の前に `npm run check` を通す。** ブラウザ試験も動かせるなら動かす
5. **設問を変えるときは、いまの設問セットを凍結してから版を上げる。**
   手順は `app/README.md` の「設問を変えるとき」。凍結し忘れると、過去の回答に
   いまの設問文が付いて記録の意味が変わる

## 6. ハマった箇所（同じ轍を踏まないために）

| 症状 | 原因 | 対処 |
|:--|:--|:--|
| ガイドが真っ白 | `.screen` は `active` が無いと `display:none` | 器に `active` を付ける |
| ヒアリング保存と申込の到達記録が両方失敗 | **リクエスト本文は一度しか読めない**。`authorize()` が読んだあと各ハンドラが読み直していた | `authorize()` が読んだ本文を呼び出し側へ渡す |
| 申込のチェックボックスが全幅で縦積み | `.field label{display:block}`（0-1-1）が `.vq-opt{display:flex}`（0-1-0）に勝つ。`.field input{width:100%}` も後勝ち | `.vq-opt` は `.field` の中に置かない。`.vq` の下に置く |
| canonical・OGP・sitemap が `http://` | `wrangler dev` が `routes` のホストを使い、scheme が http のまま | `originOf()` で scheme を自分で決める（ローカル以外は https） |
| 公開ページの漏洩チェックが誤検知 | インラインCSSに全画面のクラス名が入っている | `<style>` を除いた markup で見る |
| Admin のフォーム保存が全部 400（Origin が一致しません） | 上と同じ。`wrangler dev` は Origin ヘッダも `routes` のホストへ書き換えるが scheme は http のまま。`originOf()` の https と突き合わせると必ずずれる | **Originチェックはホストだけを比べる**。scheme は HSTS と Secure Cookie が守る |
| 絞り込みを2つ以上かけると、別の条件の値で検索される | SQLite で `?1` と `?` を混ぜた。番号無しの `?` は「それまでの最大番号＋1」になるので、`?1` を後から書くと前の条件の値を拾う | **番号付きプレースホルダを使わない。** 同じ値を必要な数だけ push する |
| Playwright から Admin の CSV を取ると 401 | `p.request` は**ブラウザではない**ので、`Secure` Cookie を http へ送らない（ブラウザは 127.0.0.1 を例外扱いする） | ページの中から `fetch(..., {credentials:'same-origin'})` で取る |
| 手動紐づけの試験が2回目から落ちる | 候補の条件が「まだ申込が紐づいていない回答」なので、試験を回すたびに候補が減る | 試験が**候補になる回答を毎回自分で作る** |

## 7. 次にやること（本番投入）

Phase 2 まででコードは揃った。ここから先は**手元のPCでしかできない作業**が中心になる。
**A → B → C の順で進める。** 順番には理由があるので、入れ替えないほうがよい。

- **A. 本番を立ち上げる**：新しいサイトが動く状態を先に作る
- **B. 旧サイトを新ドメインへ寄せる**：これをやると、スプレッドシートへの新規流入が止まる
- **C. データを移す**：**流入が止まってから**書き出す。動いている最中に書き出すと、
  書き出したあとに増えた行を取りこぼす

### A-0. 手元のリポジトリを最新にする

まだクローンしていない場合。

```
git clone https://github.com/yuki-b4/work-relation-quiz.git
cd work-relation-quiz
git checkout claude/nature-diagnosis-phase-2-0vi1fm
```

すでにクローンしてある場合。**先に `git status` を見て、編集途中のものが無いことを確かめる**
（あれば `git stash` で退避し、あとで `git stash pop` で戻す）。

```
cd （work-relation-quiz を置いた場所）
git status
git fetch origin
git checkout claude/nature-diagnosis-phase-2-0vi1fm
git pull --ff-only origin claude/nature-diagnosis-phase-2-0vi1fm
```

`--ff-only` を付けておくと、手元に別のコミットがあるときに黙って混ざらず、その場で止まる。
止まったら、何が入っているかを `git log --oneline origin/claude/nature-diagnosis-phase-2-0vi1fm..HEAD`
で見てから決める。

最後に、入っているものを確かめる。

```
git log --oneline -6
ls app/src/routes/admin.ts app/src/views/info-page.ts
```

**Node は 22 以上が要る。** wrangler 4 系が `node >=22.0.0` を要求していて、20 系だと
`Wrangler requires at least Node.js v22.0.0` で止まる。先に確かめる。

```
node -v
```

`v22`（または `v24`）で始まっていなければ、Node を入れ替える。手順は `app/README.md` の
「Node を入れ替える」。**Node 20 のサポートは2026年4月で終わっている**ので、下げるのではなく上げる。

依存は初回だけ。`.dev.vars` はGitに入っていないので、この操作で消えることはない
（本番の秘密値は `.dev.vars` ではなく `wrangler secret put` で入れる）。

```
cd app
npm install
npm run check
```

`npm run check` が通れば、Cloudflare に触る前の準備は完了。

> **`wrangler` には必ず `npx` を付ける。** このリポジトリの中にだけ入れてあって、
> グローバルには置いていない（プロジェクトごとにバージョンを固定するため）。
> `wrangler …` と打つと `command not found: wrangler` になる。
> `npm run deploy` のように `npm run` 経由なら `npx` は要らない。
>
> ```
> npx wrangler --version    # 4.128.0 と出れば、このリポジトリのものを使えている
> ```

前提：`npx wrangler login` が通っていること（`npx wrangler whoami` でメールアドレスが出ればよい）。
以下のコマンドはすべて `app/` で実行する。

> **`/privacy` `/terms` `/about` `/faq` `/contact` は実装済み**（2026-09-06）。
> 分類上は Phase 3 だが、申込フォームの同意チェックが `/privacy` にリンクしているので、
> 本番投入より前に必要だった。事業者情報も記入済み。
> **ただしプライバシーポリシーと利用規約は、まだ専門家の確認を受けていない。**
> 公開後でよいので一度は見てもらうこと。

---

### A. 本番を立ち上げる

#### A-1. 本番D1にスキーマを流す

**デプロイより先にやる。** テーブルが無い状態でデプロイすると、開いた人全員がエラーになる。

```
npm run db:migrate:remote
```

`0001_init.sql` `0002_client_request_id.sql` `0003_admin.sql` の3つが ✅ になれば成功。
確認するときは次を実行して、15 が返ればよい。

```
npx wrangler d1 execute nature-shindan --remote \
  --command "select count(*) n from sqlite_master where type='table'"
```

#### A-2. デプロイする

```
npm run deploy
```

これで Worker ができ、`wrangler.toml` の `routes` に書いた
`natur-indicator.com` と `www.natur-indicator.com` がカスタムドメインとして紐づく。
ドメインは Cloudflare Registrar で取ってあるので、同じアカウント内で完結する。

**証明書が有効になるまで数分かかる**ことがある。すぐに繋がらなくても、少し待ってから見る。

#### A-3. 秘密値を入れる

Worker ができたあとに入れる（先に入れようとすると「その Worker は無い」と聞かれる）。
`wrangler secret put` はそれ自体が新しいバージョンを作るので、**入れ直しのデプロイは要らない**。

```
npx wrangler secret put IP_HASH_SALT
```

値は長いランダム文字列。パスワードマネージャで40文字ほど作って貼る。
**このソルトは後から変えない。** 変えると、それまでに記録した `ip_hash` と繋がらなくなる。

```
npx wrangler secret put ADMIN_BOOTSTRAP_EMAIL       # 自分のメールアドレス
npx wrangler secret put ADMIN_BOOTSTRAP_PASSWORD    # 12文字以上。パスワードマネージャで作る
```

**ログイン通知**（F2-1。1アカウント運用の単一障害点への手当てなので省略しない）。
どちらか片方でよい。

```
# (a) 自分に届くURLへ通知を飛ばす。Slack の Incoming Webhook が手軽
npx wrangler secret put LOGIN_NOTIFY_WEBHOOK

# (b) Resend でメールを送る（3つそろって有効）
npx wrangler secret put RESEND_API_KEY
npx wrangler secret put LOGIN_NOTIFY_TO
npx wrangler secret put LOGIN_NOTIFY_FROM
```

未設定のままでも動くが、Admin の全ページに警告が出続ける。

#### A-4. Admin の初期アカウントを作る

`https://natur-indicator.com/admin/bootstrap` を開き、ボタンを押す。
そのあと `/admin/login` から入れることを確かめる。

入れたら、**作成用のパスワードを消す**（この2つはもう要らない）。

```
npx wrangler secret delete ADMIN_BOOTSTRAP_PASSWORD
npx wrangler secret delete ADMIN_BOOTSTRAP_EMAIL
```

消すと `/admin/bootstrap` は 404 になる。アカウントが1つでもあれば元々 404 なので二重に閉じる形。

#### A-5. 通しで1本試す

**旧サイトを寄せる前に、新しいサイトだけで最後まで動くことを確かめる。**

1. `https://natur-indicator.com/` で診断を24問受ける
2. 結果が出る。X共有ボタンの文面が `私は「◯◯」でした！` になっている
3. 読み解きガイドを終章まで進める
4. 終章から申込フォームへ進み、テストで申し込む
5. **結果のタブを閉じてから** `https://natur-indicator.com/result` を開き、
   「すでに閉じられています」が出る（F4のワンタイム表示）
6. `/admin/responses` にいま受けた1件が出て、詳細でガイド到達と申込まで見える
7. `https://natur-indicator.com/robots.txt` と `/sitemap.xml` が `https://` で出ている

確かめ終わったら、そのテスト回答は詳細画面の「完全に消す」で消しておく。

#### A-6. Cloudflare 側の設定

ダッシュボードで `natur-indicator.com` のゾーンを開いて、3つ。

- **HSTS を有効にする**（6.1）。SSL/TLS → エッジ証明書 → HSTS
- **AI クローラーの制御**（F8・確認事項11）。学習目的のクローラーだけをブロックし、
  **AI検索・回答は通す**。メニュー名はしばしば変わるので、セキュリティまわりで
  「AI Crawl Control」「AI Scrapers and Crawlers」といった項目を探す
- **使用量アラート**（8.5）。通知（Notifications）から Workers の使用量アラートを作る。
  リクエストが跳ねたときに気づける

---

### B. 旧サイトを新ドメインへ寄せる

いまも GitHub Pages が動いていて、そこから受けた回答は Apps Script 経由でスプレッドシートに
入り続けている。**ここを止めないと、Cで書き出したあとに増えた行が取り残される。**

#### B-1. `index.html` を新ドメインへのリダイレクトに変える

いまは `prototype.html` へ飛ばしているのを、`https://natur-indicator.com/` へ変える。
`?ref=` などのクエリはそのまま引き継ぐ。

#### B-2. `prototype.html` の直リンクをどうするか（判断が要る）

`prototype.html` は**アプリの文面の正**で、`npm run content` がここから機械抽出している。
プレビューURLの提示ルールもこのファイルを見ている。
**リダイレクトを仕込むと、抽出とプレビューの両方が壊れる**ので、ここは触らない。

そのぶん `.../prototype.html?ref=...` の直リンクは生き続け、スプレッドシートに書き込み続ける。
選べるのは2つ。

- **(推奨) GitHub Pages を止める。** リポジトリの設定で Pages を無効にすると、旧URLは404になる。
  拡散済みのリンクは切れるが、`index.html` 経由の導線は B-1 で新ドメインへ繋がっている
- **(様子見) しばらく残す。** その場合、Cの書き出しは「新しい行がもう来ない」と確認してから行う

要件7.2は「流入がゼロになったら停止する」なので、**急がないなら様子見でよい**。
どちらにしても、Cの直前に**シートの最終行の日時を見て、新規が止まっていること**を確かめる。

#### B-3. Apps Script はまだ止めない

旧URLが残っているあいだは受け口として残す（7.2）。止めるのは流入がゼロになってから。

---

### C. データを移す（第7章）

#### C-1. 4つのタブをCSVで書き出す

回答ログのスプレッドシートで、タブごとに
**ファイル → ダウンロード → カンマ区切り形式（.csv）**。
Googleスプレッドシートは**開いているタブだけ**を書き出すので、タブを切り替えて4回行う。

| タブ | 移行先 |
|:--|:--|
| `指標v2` | `responses` ＋ `response_answers` ＋ `feedback_surveys` ＋ `hearings` |
| `法人リード` | `corp_leads` |
| `紹介者マスタ` | `referrers` |
| Googleフォームの回答（「フォームの回答 1」など） | `session_applications` |

4つを1つのフォルダ（例 `app/export/`）に入れる。**ファイル名は変えなくてよい。**
`ブック名 - シート名.csv` という既定の名前にタブ名が入っているので、スクリプトが拾う。

> 数式の心配は要らない。CSVの書き出しは計算結果の値になるうえ、移行スクリプトは
> `紹介者名` 列を読まない（`紹介元` のコードを紹介者マスタから引き直す）。

#### C-2. 投入せずに、まず数える

```
npm run migrate -- --dir ./export --check
```

シートの行数・取り込む行数・タイプ別の分布・日付の範囲が出る。
**ここでシート側の件数と突き合わせる**（7.2の手順2）。取り込まない行があれば理由が並ぶので、
直せるものは直す。

#### C-3. ローカルで一度流して、Admin で見る

```
npm run migrate -- --dir ./export --out migrate.sql
npx wrangler d1 execute nature-shindan --local --file=migrate.sql
npm run dev
```

`http://localhost:8787/admin/responses` で、件数・タイプの分布・紹介者名の解決・
移行データの詳細（設問が9問だけで「設問文は保存されていません」と出る）を確かめる。
`/admin/sessions` で未紐づけの申込がいくつあるかも見ておく。

#### C-4. 本番へ流す前に、戻れるようにしておく

```
npx wrangler d1 time-travel info nature-shindan
```

出てきたタイムスタンプを控える。失敗しても
`npx wrangler d1 time-travel restore nature-shindan --timestamp=...` で戻せる。

#### C-5. 本番へ流して、もう一度照合する

```
npx wrangler d1 execute nature-shindan --remote --file=migrate.sql
```

`https://natur-indicator.com/admin/responses` で C-3 と同じ確認をする。

> 移行スクリプトはすべて `INSERT OR IGNORE` なので、**何度流しても増えない**。
> あとからシートに行が増えた場合は、書き出し直して流せば差分だけ入る。
> ただし入るのは**増えた行だけ**で、既存行の書き換え（あとから備考を書いた等）は反映されない。

#### C-6. 締める

1. **Googleフォームの受付を締め切る**（確認事項3＝a）。フォームの「回答」タブで
   「回答を受け付ける」をオフにし、締め切りメッセージに新しい申込先（`/apply/...` へ至る導線）を書く
2. スプレッドシートを**読み取り専用**にする（削除はしない。7.2の手順3）
3. `/admin/sessions` で未紐づけの申込を手で繋ぐ。候補が「同じタイプで直前に到達した回答」として
   出るので、選ぶだけで済む

---

### D. そのあとに残るもの（Phase 3 以降）

- Search Console と Bing Webmaster Tools の登録、sitemap 送信、初期順位の記録
- **プライバシーポリシーと利用規約を専門家に確認してもらう**（文面と事業者情報は入っている）
- OGP画像（1200×630）の作成。いま `og:image` を出していないので、共有時のカードが寂しい
- `POST /api/corp-leads` の実装と法人LP 3枚の移設（Phase 4）
- 保存期間3年をどう守るか（6.2）。自動削除を作らないので、ポリシーの表現を「3年を目安に」に
  するか、年1回 Admin の削除導線でまとめて消すかを決める
