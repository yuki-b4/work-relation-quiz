# 体験セッション申込フォーム（Googleフォーム）たたき v1

読み解きガイド終章の申込ボタン（`prototype.html` の `SESSION_FORM_URL`）から別タブで開く外部フォーム。
**この文書がフォームの正**。文言を変えるときはここを先に直し、フォーム側へ反映する。

## 設計方針

- **入力は2分以内**。ガイドを読み切って温まった状態で開くので、ここで冷まさない。必須は3問だけにする。
- **アプリで取れる情報は聞き直さない**。診断タイプ・紹介元・商談前ヒアリング3項目は、すでに回答ログに入っている（`?ref=` と回答IDで紐づく）。
- ただし**現状はフォームとログを突き合わせる鍵がない**ため、暫定でタイプと悩みだけ最小限に持つ。プレフィル（下の「回答ログとの紐づけ」）を入れたら、この2問は削ってよい。
- 「まずは申し込みだけでもOK」と本文で言っているので、**任意項目は空のまま送信できる**ことが分かる書き方にする。

## フォーム設定

| 項目 | 値 |
|:--|:--|
| タイトル | 体験セッション（30〜45分・無料）お申し込み |
| メールアドレスを収集する | オフ（質問として聞く。Googleログインを必須にしない） |
| 進行状況バー | オフ |
| 回答の編集を許可 | オフ |
| 回答は1回に制限 | オフ（ログイン必須になるため） |
| 回答先 | 「職場の人間関係タイプ診断_回答ログ」スプレッドシートに新規シートを作成 |
| 確認メッセージ | お申し込みありがとうございます。<br>2営業日以内に、日程のご連絡を差し上げます。<br>迷惑メールフォルダに入ることがあるので、あわせてご確認ください。 |

## 冒頭の説明文

> 読み解きガイドを最後まで読んでくださり、ありがとうございます。
>
> このセッションでは、あなたの診断結果をもとに、いま何が起きているのか、次に何をしたら良いのかを一緒に読み解きます。
>
> オンラインで30〜45分。費用はかかりません。
> セッションの最後に、その先の進め方のご案内にも少しだけお時間をいただきます。
>
> 入力は2分ほどで終わります。答えにくい項目は、空のままで大丈夫です。

## 質問

### 1. お名前 ※必須

- 形式：記述式（短文）
- 補足：ニックネームでも構いません。当日お呼びする名前を教えてください。

### 2. メールアドレス ※必須

- 形式：記述式（短文）＋メールアドレスの検証
- 補足：日程のご連絡に使います。

### 3. 診断結果のタイプ ※必須

- 形式：プルダウン
- 補足：読み解きガイドの表紙、または診断結果の画面に出ているタイプです。
- 選択肢：突撃隊長／正論ハンマー／お祭り隊長／自由人コメンテーター／沈黙の大黒柱／縁の下の職人／根回しの仕掛け人／がんばり屋の調整役／わからない・覚えていない

### 4. いま、人間関係で気になっていること（任意）

- 形式：記述式（段落）
- 補足：ひと言でも大丈夫です。書いていただけると、当日の読み解きが早く、深くなります。

### 5. 希望の時間帯（任意・複数選択可）

- 形式：チェックボックス
- 補足：候補をいくつか選んでいただけると、日程の調整が早く済みます。
- 選択肢：平日の午前／平日の午後／平日の夜（19時以降）／土日の午前／土日の午後／土日の夜／その他（自由記述）

### 6. ご質問・伝えておきたいこと（任意）

- 形式：記述式（段落）

### 7. 診断ID（任意）

- 形式：記述式（短文）
- 補足：自動で入力される場合があります。空のままで大丈夫です。
- 用途：回答ログの行と突き合わせるための鍵。手入力は想定しない。

## 回答ログとの紐づけ（次の一手）

いまはフォームの回答と診断の回答ログが別々に貯まる。**プレフィル**を入れると1本につながる。

1. フォーム作成後、質問7のプレフィル用パラメータ（`entry.〇〇〇〇`）を控える
   （`apps-script/CreateSessionForm.gs` を実行すると実行ログに出る）
2. `prototype.html` の申込リンクを `SESSION_FORM_URL + '?usp=pp_url&entry.〇〇〇〇=' + 回答ID` の形にする
3. これで「診断のタイプ・5軸・商談前ヒアリング」と「申込者の氏名・連絡先」が突き合わせられる
4. 突き合わせが効くようになったら、質問3と4はフォームから外してよい（アプリ側で取得済みのため）

## 作り方

`apps-script/CreateSessionForm.gs` を Apps Script に貼って `createSessionForm()` を一度だけ実行すると、
この仕様どおりのフォームが作られる。実行ログに以下が出るので控える。

- 公開URL（`prototype.html` の `SESSION_FORM_URL` に貼る）
- 編集URL
- 質問7のプレフィル用URL（`entry.〇〇〇〇` を含む）

手で作る場合も、この文書の文言をそのまま使う。
