# ナチュール診断 アプリケーション化 要件定義（v2）

策定日：2026-09-02 ／ 確認事項10件の確定を反映：2026-09-02（v2）
対象：GitHub Pages の静的ページからアプリケーションへの移行

## 0. この文書の位置づけ

現在 GitHub Pages に静的ページとしてホストしている診断を、DBとAdmin画面を持つアプリケーションへ移行するための要件をまとめたもの。**この文書がアプリ化の要件の正**とし、方針を変えるときはここを先に更新する。

実装フェーズに入ったら、画面ごとの詳細仕様は本書から分割してよい。診断の文面（設問・タイプ・トリセツ・ガイド本文）の正は従来どおり `prototype.html`／`ガイド文面24本.md`／`guide-3day-spec.md` 側にあり、本書はそれを移植する前提の器の要件を扱う。

**v2 で確認事項10件がすべて確定した**（第10章）。確定内容は各要件へ反映済みで、未決の論点は残っていない。

### 要求と要件の対応

| # | いただいた要求 | 対応する要件 |
|:--|:--|:--|
| 1 | スプレッドシート記録を全てDB管理に | F1 |
| 2 | Admin画面で回答結果と体験セッション申込を回答毎に閲覧 | F2 |
| 3 | 回答画面の導線・設問・ロジック・結果UIは踏襲／閉じたら再表示不可 | F3・F4 |
| 4 | 回答したデバイス・ブラウザ以外では結果を見られない | F4 |
| 5 | 結果画面にX共有リンク（定型文プリセット） | F5 |
| 6 | 回答画面はオーガニック検索に出す／Adminはnoindex | F6 |
| 7 | 「ナチュール診断」の指名検索SEO対策 | F7 |

## 1. 背景と目的

### 背景

診断は無料の共通入口かつ共通言語であり、収益はその先の人的サービス層（体験セッション・継続伴走・ワークショップ）で立てる。したがって診断そのものより、**「誰が・どのタイプで・どこから来て・申込まで進んだか」を一人ずつ追えること**が事業上の価値になる。

現状はこれがスプレッドシートに散っており、体験セッションの申込（Googleフォーム）とも別管理のため、一人の回答者を通しで見ることができない。

### 目的

1. 回答から体験セッション申込までを、**一意のIDで通しで追える**状態にする（DB化）
2. スプレッドシートを開かずに、**Admin画面だけで日々の運用が回る**状態にする
3. 診断結果を**回答者本人・その場限り**のものにして、結果の価値と体験の一回性を守る
4. 「ナチュール診断」という**ブランド名で検索されたときに必ず受け止められる**状態にする

### 非目的

- 診断ロジック・設問・タイプ判定・結果画面のUIを変えること（現行を踏襲する）
- 診断そのものを課金対象にすること
- 回答者向けのアカウント機能（ログイン・マイページ・履歴）を作ること

## 2. スコープ

### 対象に含む（IN）

- 診断アプリ本体（イントロ／フレーム／設問24問／結果／読み解きガイド全4章）
- 体験セッション申込フォーム（アプリ内・独立URL。確認事項3＝a／4＝C）
- Admin画面（回答一覧・回答詳細・体験セッション申込・法人リード・紹介者マスタ）
- データベース（回答・設問回答明細・検証アンケート・ヒアリング・申込・法人リード・紹介者・管理ユーザー）
- 既存スプレッドシートからのデータ移行
- 全8タイプ一覧（現 `all-types.html`）のアプリ内への移設
- SEO基盤（SSR・メタ情報・構造化データ・sitemap・robots・OGP）
- 法人LP 3枚（`lp-corporate` / `lp-wedding` / `lp-coffee`）の移設

### 対象に含まない（OUT）

- 読み解きガイドのメール配信（2026-07-27に停止済み。`apps-script/Guide.gs` は温存し、アプリには移植しない）
- 事業ロードマップ用スクリプト（`UpdateRoadmapSheets.gs`）。ロードマップはスプレッドシートのまま
- 回答者向けの再閲覧機能（要求3・4と真っ向から矛盾するため作らない）
- 決済・契約管理
- 集計ダッシュボード（確認事項10＝b。CSVエクスポートで代替する）
- 検証アンケート5問（`?mode=feedback`）とセグメント出し分け（確認事項9＝b。廃止する）

### 確定した判断（第10章に記録）

確認事項10件はすべて確定した。スコープに効くものは次のとおり。

- **体験セッション申込フォームはアプリ内に内製化する**（確認事項3＝a）。Googleフォームは廃止する
- **独自ドメインを取得する**（確認事項5＝a）
- **検証モード（`?mode=feedback`）とセグメント出し分けは廃止する**（確認事項9＝b）。`?ref=` は記録のためだけに残す（F3-1の注記）
- **申込フォームだけワンタイムの外に出す**（確認事項4＝C）。結果とガイド本文はワンタイムのまま
- **回答データの保存期間は3年**（確認事項7）
- **Adminは1名運用・権限分離なし**（確認事項8）
- **集計ダッシュボードは作らない**（確認事項10＝b）。CSVエクスポート＋スプレッドシートで足りる

## 3. 現状（As-Is）

### 3.1 画面と導線

`prototype.html` 1ファイルにすべてが入っている。画面は5つで、DOMの表示切り替えで進む（URLは変わらない）。

| 画面 | id | 内容 |
|:--|:--|:--|
| イントロ | `intro` | 診断の説明3ステップと開始ボタン |
| フレーム | `frame` | 「特定の場面ではなく普段のあなたで」の教示。回答のブレ防止 |
| 設問 | `quiz` | 全24問。9問（二択・タイプ判定）→ 15問（4件法リッカート・5軸） |
| 結果 | `result` | タイプカード、3軸、あるある、強み、トリセツ、5軸、深層、CTA、検証アンケート |
| 読み解きガイド | `guide` | 全4章（序章・第一章・第二章・終章）。終章に商談前ヒアリングと体験セッション申込 |

判定ロジック：

- **タイプ判定**：Q1〜Q9を3軸（本音 h／衝突 c／重心 w）に3問ずつ割り当て、各軸で多数決（`L>=2`）。`O|G` × `B|K` × `L|S` の8通りのコードになる
- **5軸スコア**：Q10〜Q24を5軸（safety / trust / bound / conflict / connect）に3問ずつ割り当て、平均を0〜1に正規化（`(平均-1)/3`）
- **出し分け**：`?ref=` からセグメント（`general` / `referral` / `leader-lp` / `member`）を判定。読み解きガイドのCTAは `referral` のみ。検証アンケート5問は `?mode=feedback` のときだけ

### 3.2 データの記録先（すべてスプレッドシート）

記録先は「職場の人間関係タイプ診断_回答ログ」1冊（ID: `1J9sfu2F3uJtpriORSkz7v-5uNQHD6mR4j3UZ22H1Q7M`）で、Apps Script Web App（`apps-script/Code.gs`）が受け口になっている。

| 記録先 | 内容 | 書き込み元 |
|:--|:--|:--|
| `指標v2` シート | 診断結果・9問の回答・3軸判定・5軸スコア・検証アンケート・商談前ヒアリング・セグメント | `action:create` / `action:update` |
| `法人リード` シート | 法人LPの商談フォーム | `action:corpLead` |
| `紹介者マスタ` シート | 紹介者コード → 紹介者名 | 手入力。`指標v2` から VLOOKUP で逆引き |
| Googleフォーム回答シート | 体験セッション申込（氏名・メール・希望時間帯など） | Googleフォームから直接 |
| `ガイド文面` / `ガイド設定` シート | メール配信用（**停止中**） | `Guide.gs` |

記録の流れは2フェーズ：診断完了時に回答IDつきの行を先に追加し、アンケート回答時に同じ行を更新する。

### 3.3 現状の課題

| # | 課題 | 影響 |
|:--|:--|:--|
| 1 | 診断ログとセッション申込が別シートで、突き合わせが回答IDの手作業 | 誰が申し込んだか一目で分からない |
| 2 | 列の並びが `Code.gs` の `HEADERS` とシートの1行目の両方に依存。ズレるとデータが別列に入る | 列追加のたびに再デプロイが必要で壊れやすい |
| 3 | 「紹介者名」「対応状況」「メモ」「備考」が手入力列。数式（VLOOKUP）と手入力が同じシートに混在 | 行の並べ替え・削除で壊れる |
| 4 | 15問のリッカート回答（Q10〜Q24）の生値が保存されていない（5軸スコアだけ） | 設問ごとの分布を後から検証できない |
| 5 | `workplaceFrame`（回答アンカー）は送信されているが列がなく捨てられている | 記録漏れ |
| 6 | 回答IDをクライアントで生成している（`crypto.randomUUID`） | 偽装・重複の余地がある |
| 7 | Apps Script が `mode:'no-cors'` 受けで、成功/失敗をクライアントが判定できない | 記録漏れに気づけない |
| 8 | 結果画面がクライアント内で完結し、URLを知らなくても同じ画面に戻れる（リロードで消えるだけ） | 一回性の担保がない |
| 9 | `index.html` が JavaScript の `location.replace` で `prototype.html` へ転送 | 検索エンジンに評価されない。実質インデックス不可 |
| 10 | メタディスクリプション・OGP・構造化データ・sitemap・robots.txt がすべて無い | 指名検索で受け止められない |
| 11 | Apps Script のアクセスが「全員」で公開URL | 不特定多数に配る段階では保護が必要 |
| 12 | 全8タイプ一覧が `all-types.html` として孤立し、診断本体からの導線が別タブリンクのみ | 回遊とSEOの両方で機会損失 |

## 4. 全体像（To-Be）

### 4.1 構成の方針

```
[ 回答者 ]                              [ 運営 ]
    |                                       |
    v                                       v
 公開アプリ（SSR）                    Admin（認証必須・noindex）
  /            トップ・診断              /admin/responses      回答一覧
  /types       全8タイプ                /admin/responses/:id  回答詳細
  /types/:code タイプ個別               /admin/sessions       体験セッション申込
  /result      結果（ワンタイム）        /admin/corp-leads     法人リード
  /guide       読み解きガイド            /admin/referrers      紹介者マスタ
  /for-business 法人LP                  /admin/export         CSV出力
    |                                       |
    +-------------> [ Database ] <----------+
```

原則：

- **記録の唯一の正はDB**にする。スプレッドシートは移行後、読み取り専用のアーカイブとして残す（二重運用しない）
- **公開ページはサーバ側でHTMLを生成**する（SSR/SSG）。JavaScriptの実行を待たないと本文が出ない構造にしない（F6の前提）
- **結果とガイドはサーバ側で認可**する。クライアントの状態だけで表示可否を決めない（F4の前提）
- **Adminは公開アプリと同一ドメインのパス配下**に置く（Cookieのスコープが素直になる。サブドメイン分離でも可）

### 4.2 画面一覧とURL設計

| URL | 画面 | インデックス | 認証 |
|:--|:--|:--:|:--:|
| `/` | トップ（診断の説明・開始） | index | 不要 |
| `/quiz` | 設問（24問） | noindex（canonical は `/`） | 不要 |
| `/result` | 結果（**IDを含まない**） | noindex, nofollow | 結果セッション |
| `/guide` | 読み解きガイド全4章 | noindex, nofollow | 結果セッション |
| `/apply/{token}` | 体験セッション申込フォーム | noindex, nofollow | 申込トークン（F4-5） |
| `/types` | 全8タイプ一覧（現 `all-types.html`） | index | 不要 |
| `/types/{code}` | タイプ個別（8ページ・新規） | index | 不要 |
| `/about` | 診断について（監修・背景） | index | 不要 |
| `/faq` | よくある質問 | index | 不要 |
| `/privacy` | プライバシーポリシー | index | 不要 |
| `/terms` | 利用規約 | index | 不要 |
| `/contact` | お問い合わせ | index | 不要 |
| `/for-business` ほか | 法人LP 3枚 | index | 不要 |
| `/admin/**` | Admin | **noindex, nofollow** | 必須 |
| `/api/**` | API | noindex | 種別による |

補足：

- `/quiz` を別URLにするか、`/` 上で進行させるかは実装判断。別URLにする場合は canonical を `/` に向け、noindex にする（内容が空のため）
- `/result` に**回答IDを含めない**のは要求4の中核。URLを共有しても他人が開けない状態にするため（F4-3）
- `/apply/{token}` は**唯一、ワンタイムの外に出る画面**。確認事項4＝C の決定による。診断結果は一切表示しない（F4-5）
- 旧URL（`/prototype.html`・`/all-types.html`・`/index.html`）は新URLへ**301リダイレクト**する

## 5. 機能要件

### F1 データのDB化（要求1）

#### F1-1 方針

- 現在スプレッドシートに記録している項目を**すべて**DBに移す。対象は `指標v2`・`法人リード`・`紹介者マスタ`・Googleフォーム回答シートの4つ
- 現状で捨てられている項目（15問のリッカート生値、`workplaceFrame`）も**取りこぼさず保存する**
- 回答IDは**サーバ側で採番**する（UUID v4）。クライアント生成をやめる
- スプレッドシートの「手入力列」（紹介者名・対応状況・メモ・備考）は、Adminから編集できるDBの項目に置き換える

#### F1-2 エンティティ

**`responses`（回答）** 1診断＝1行

| 項目 | 型 | 現行の対応 | 備考 |
|:--|:--|:--|:--|
| `id` | uuid PK | 回答ID | サーバ採番 |
| `created_at` | timestamptz | タイムスタンプ | |
| `completed_at` | timestamptz | なし | 24問完了時刻 |
| `question_set_version` | text | なし | 設問改訂に備える。**必須** |
| `mode` | text | モード | 移行データの互換用。新規は `general` 固定（確認事項9＝b） |
| `segment` | text | セグメント | 移行データの互換用。新規は `general` 固定（同上） |
| `ref_code` | text | 紹介元 | 生の `?ref=` 値 |
| `referrer_id` | FK → referrers | 紹介者名（数式） | 解決できた場合のみ |
| `frame` | text | （捨てられていた） | `workplaceFrame`。現在は固定値「自然体」 |
| `type_code` | text | タイプコード | OBL / OBS / OKL / OKS / GBL / GBS / GKL / GKS |
| `type_name` | text | タイプ名 | |
| `axis_h` / `axis_c` / `axis_w` | char(1) | 判定_本音／衝突／重心 | O\|G / B\|K / L\|S |
| `axis_h_count` ほか | jsonb | なし | 各軸の左右カウント。境界事例の分析用 |
| `radar_safety` ほか5項目 | numeric(4,3) | 5軸_本音 ほか | 0〜1 |
| `entry_url` | text | なし | 流入URL（クエリ込み） |
| `referrer_url` | text | なし | HTTPリファラ |
| `utm_source` ほか | text | なし | 流入計測 |
| `device_type` / `os` / `browser` | text | なし | UAから導出（生UAも別途保存） |
| `ip_hash` | text | なし | **生IPは保存しない**。ソルト付きハッシュのみ |
| `shared_at` / `share_count` | timestamptz / int | なし | X共有ボタンのクリック（F5-5） |
| `guide_opened_at` / `guide_max_chapter` / `guide_completed_at` | | なし | 読み解きガイドの到達計測 |
| `purge_after` | date | なし | 保存期間3年（6.2）の自動削除の基準日 |
| `admin_status` | text | 対応状況（手入力） | Admin編集 |
| `admin_note` | text | 備考・メモ（手入力） | Admin編集 |
| `deleted_at` | timestamptz | なし | 削除依頼対応の論理削除 |

**`response_answers`（設問別回答）** 1診断＝24行

| 項目 | 型 | 備考 |
|:--|:--|:--|
| `response_id` | FK | |
| `order_no` | int | 1〜24（出題順） |
| `question_key` | text | 設問の識別子 |
| `kind` | text | `bin`（二択）/ `lik`（4件法） |
| `axis` | text | h / c / w / safety / trust / bound / conflict / connect |
| `value` | text | 二択は極（O/G/B/K/L/S）、リッカートは 1〜4 |
| `answered_at` | timestamptz | 設問ごとの所要時間の分析用（任意） |

> 現行は9問しか保存していない。**15問のリッカート生値もここに入れる**ことで、設問ごとの分布・偏り・所要時間を後から検証できるようになる。

**`feedback_surveys`（検証アンケート）** **新規の書き込みは行わない**（確認事項9＝b で廃止）。既存データの移行先としてのみ作り、Adminでは過去の回答を参照できる。

`me` / `others` / `others_who` / `share` / `share_who` / `dig` / `miss`

**`hearings`（商談前ヒアリング）** responses と 1:0または1:1

`now_text`（いま悩んでいること。現行の「自由記述」）/ `future_text`（解消後の毎日。現行の「理想_解消後の毎日」）/ `updated_at`

**`session_applications`（体験セッション申込）**

| 項目 | 型 | 備考 |
|:--|:--|:--|
| `id` | uuid PK | |
| `response_id` | FK → responses（nullable） | **要求2の要。ここが繋がって初めて回答毎に見える** |
| `created_at` | timestamptz | |
| `name` / `email` | text | 必須 |
| `type_selected` | text | 自己申告のタイプ（内製化すれば不要） |
| `concern` | text | いま気になっていること |
| `preferred_slots` | text[] | 希望の時間帯（複数） |
| `question` | text | 質問・伝えておきたいこと |
| `source` | text | `in-app` / `google-form-import` |
| `status` | text | 未対応／日程調整中／実施済／成約／辞退 |
| `held_at` | timestamptz | 実施日 |
| `admin_note` | text | Admin編集 |

**`corp_leads`（法人リード）**

`created_at` / `email` / `issues`（text[]。現行は「、」連結の1セル）/ `detail` / `ref_code` / `page` / `lead_id` / `status` / `admin_note`

**`referrers`（紹介者マスタ）**

`code` PK（例 `TKtp46k`）/ `name` / `note` / `active` / `created_at`

**`admin_users`** `id` / `email` / `password_hash` / `last_login_at` / `disabled_at`

> 確認事項8で **1名運用・権限分離なし**と確定したため、`role` は持たない。増員が必要になった時点で追加する。

**`admin_audit_logs`** `actor_id` / `at` / `action` / `target_type` / `target_id` / `ip_hash` / `diff`

**`result_sessions`（結果閲覧セッション）** F4の実体。詳細はF4-2

**`apply_tokens`（申込トークン）** 確認事項4＝C の実体。詳細はF4-5

`token` PK（推測不能なランダム値）/ `response_id` FK / `issued_at` / `expires_at` / `used_at` / `revoked_at`

#### F1-3 API

| メソッド | パス | 内容 |
|:--|:--|:--|
| POST | `/api/responses` | 24問の回答を送信。responses と response_answers を作成し、結果セッションを発行 |
| POST | `/api/responses/:id/feedback` | 検証アンケート |
| POST | `/api/responses/:id/hearing` | 商談前ヒアリング（変更のたびに保存） |
| POST | `/api/session-applications` | 体験セッション申込。`token` で回答に紐づける（F4-5） |
| POST | `/api/corp-leads` | 法人LPの商談フォーム |
| POST | `/api/result/close` | 結果セッションを明示的に閉じる（F4-4） |
| POST | `/api/guide/progress` | 読み解きガイドの到達章を記録 |

要件：

- すべて**同一オリジン**。`no-cors` をやめ、**成功・失敗をクライアントが判定できる**ようにする（現状の課題7の解消）
- 失敗時は自動リトライ（指数バックオフ・最大3回）し、それでも失敗したら画面に明示する
- 二重送信防止：`POST /api/responses` は冪等キー（クライアント生成のリクエストID）を受け取り、重複は同じ結果セッションを返す
- レート制限：IP単位で `POST /api/responses` を毎分N件までに制限（いたずら投稿対策）
- 送信ペイロードのバリデーション（設問数・値域・タイプコードの整合）をサーバ側で行う。**タイプ判定と5軸スコアはサーバ側で再計算し、クライアントの申告値を信用しない**

### F2 Admin画面（要求2）

#### F2-1 認証・保護

- メールアドレス＋パスワード認証。パスワードは bcrypt / argon2 でハッシュ化
- ログイン試行回数制限（5回失敗で一定時間ロック）
- セッションは HttpOnly / Secure / SameSite=Lax Cookie。無操作30分でタイムアウト
- 全 `/admin/**` レスポンスに `X-Robots-Tag: noindex, nofollow` と `Cache-Control: no-store`（F6-3）
- 操作ログ（`admin_audit_logs`）を残す。特に個人情報の閲覧・エクスポート・削除
- 二要素認証は将来。当面は強固なパスワード＋ログイン通知で運用（第11章）

> **1名運用ゆえの注意（確認事項8）**：アカウントが1つしかないため、それが破られると全データが露出し、パスワードを忘れると誰も入れなくなる。権限分離は不要でも、次の3つは省略しない。
> 1. パスワードマネージャで生成した長いパスワードを使う
> 2. ログイン成功時にメール通知を出す（身に覚えのないログインに気づける）
> 3. 復旧手段を用意する（メールによるパスワード再設定、または管理者が直接DBを操作する手順の文書化）
>
> 監査ログ（`admin_audit_logs`）は「誰が」を区別する必要がなくなるが、**個人情報へのアクセス記録として引き続き残す**。

#### F2-2 回答一覧 `/admin/responses`

表示列：日時／タイプ（コード＋名前）／紹介元コード／紹介者名／流入元（`src`）／ガイド到達／**申込有無**／対応状況

機能：

- 絞り込み：期間、タイプ、紹介元、流入元、申込の有無、対応状況、フリーワード（ヒアリング本文）
- 並べ替え：日時、タイプ、申込日時
- ページング（既定50件）
- CSVエクスポート（絞り込み条件を反映）
- 一覧から詳細へ

#### F2-3 回答詳細 `/admin/responses/:id`

**一画面で、その人のすべてが見えること**を要件とする。ブロック構成：

1. **基本情報**：回答ID、回答日時、所要時間、紹介元コードと紹介者名、流入URL、UTM、デバイス種別・OS・ブラウザ、設問セットのバージョン
2. **診断結果**：タイプコード・タイプ名・キャッチ、3軸の判定と内訳カウント、5軸スコア（回答画面と同じレーダー表示）
3. **設問別回答（24問）**：出題順に、**設問文・選んだ選択肢・軸**を並べる。設問文は回答時のバージョンのものを表示する
4. **検証アンケート**：移行データにあれば5問の回答（新規収集は廃止。確認事項9＝b）
5. **商談前ヒアリング**：あれば2項目の本文
6. **読み解きガイド到達**：開いたか／最終到達章／終章到達日時
7. **体験セッション申込**：あれば氏名・メール・希望時間帯・質問・ステータス・実施日。無ければ「申込なし」
8. **運用欄**：対応状況（プルダウン）・メモ（自由記述）を**Adminから編集・保存**できる

回答者本人の再閲覧は不可（F4）だが、**Adminは何度でも閲覧できる**。これは矛盾しない（DBの記録と、本人向けのワンタイム表示は別物）。

#### F2-4 体験セッション申込一覧 `/admin/sessions`

申込側からの入口。列：申込日時／氏名／メール／紐づく回答（タイプ・回答日時へのリンク）／希望時間帯／ステータス／実施日。ステータスは一覧から直接変更できる。

**回答に紐づいていない申込**は明示し、Adminから手動で回答に紐づけられること。新規の申込は `/apply/{token}` から入るため必ず紐づくが、次の2つは紐づかないまま入る。

- Googleフォームからの移行分（「診断ID」が空の行）
- 申込URLを人づてに受け取った第三者からの申込（F4-5の制約）

#### F2-5 法人リード一覧 `/admin/corp-leads`

列：日時／メール／課題／自由記述／流入ページ／紹介元／ステータス／メモ。ステータスとメモはAdmin編集。

#### F2-6 紹介者マスタ `/admin/referrers`

- 一覧・追加・編集・無効化
- **紹介リンクの自動生成**：コード（イニシャル英大文字＋ランダム英数字5文字）を発行し、`https://（ドメイン）/?ref=XXxxxxx` をコピーできる
- 紹介者ごとの実績（回答数・ガイド到達数・申込数）を表示

> 現在スプレッドシート手作業＋VLOOKUPで運用している部分。ここをAdmin化すると、コードの重複・大小違いの取り違えといった事故がなくなる。

#### F2-7 エクスポート `/admin/export`

- 回答（1行1回答）／設問別回答（1行1設問）／申込／法人リードの4種
- 期間指定・UTF-8 BOM付きCSV（Excelでの文字化け防止）
- エクスポート操作は監査ログに残す

### F3 回答画面（要求3・現行踏襲）

#### F3-1 踏襲するもの

以下は**現行 `prototype.html` の挙動をそのまま移植する**。変更する場合は本書を先に更新する。

| 対象 | 踏襲内容 |
|:--|:--|
| 画面導線 | イントロ → フレーム → 設問24問 → 結果 → 読み解きガイド全4章 →（終章から）体験セッション申込 |
| 設問 | 9問（二択）＋15問（4件法リッカート）。文面・選択肢・出題順・軸の割り当て |
| 判定ロジック | 3軸の多数決による8タイプ判定、5軸の平均正規化（0〜1） |
| 結果画面UI | タイプカード（エンブレム・キャッチ・タイプ名・コード）、3軸、あるある、強み、トリセツ、5つの関わり方傾向、特徴的な傾向、深層、CTA、全8タイプへの導線、もう一度診断する |
| 読み解きガイド | 本の体裁、章送り、章立て（序章・第一章・第二章・終章）、終章の商談前ヒアリングと体験セッション申込 |
| 表示挙動 | 画面・設問が切り替わるたびに最上部へスクロール、進捗バー、ひとつ戻る |
| 配色・書体 | 現行のトークン（coral / teal / trust、Noto Sans JP / Noto Serif JP） |

#### F3-2 移行にあたって変えるもの

| 変更 | 理由 |
|:--|:--|
| 回答IDをサーバ採番にする | 偽装・重複の防止（課題6） |
| 送信を同一オリジンAPIにし、成否を判定する | 記録漏れの検知（課題7） |
| 15問のリッカート生値を保存する | 検証可能性（課題4） |
| `workplaceFrame` を保存する | 記録漏れの解消（課題5） |
| タイプ判定と5軸スコアをサーバ側で再計算する | クライアント申告値を信用しない |
| 全8タイプへの導線をアプリ内ページ（`/types`）にする | 別タブの静的HTMLをやめ、回遊とSEOを両立（課題12） |
| 結果表示をサーバ認可にする | F4の前提（課題8） |
| セグメント出し分けと検証アンケートを廃止する | 確認事項9＝b（F3-3） |
| 体験セッション申込をアプリ内の `/apply/{token}` にする | 確認事項3＝a・4＝C（F4-5） |

#### F3-3 セグメント出し分けの廃止（確認事項9＝b）

現行の4セグメント（`general` / `referral` / `leader-lp` / `member`）による画面の出し分けを**やめ、全員に同じ画面を出す**。

| 対象 | 現行 | アプリ版 |
|:--|:--|:--|
| 読み解きガイドのCTA | `referral` のみ | **全員に出す** |
| 深層の「開いたループ」 | `referral` のみ | **全員に出す** |
| 商談前ヒアリング2項目 | `referral` のみ | **全員に出す**（すべて任意のまま） |
| 検証アンケート5問 | `?mode=feedback` のみ | **廃止**（画面から削除） |
| `?seg=` による上書き | あり | 廃止 |

**`?ref=` は記録のためだけに残す。** 画面の分岐には使わないが、紹介者コードとして受け取り、`responses.ref_code` と `referrer_id` に記録する。紹介者マスタ（F2-6）と紹介者ごとの実績集計は、これがないと成立しないため。X共有からの流入は `?src=x`（F5-3）で別に受ける。

> **確認事項9の解釈**：「廃止」は**画面の出し分けの廃止**と読み、**流入元の記録は残す**ものとした。F2-6（紹介リンクの自動生成と紹介者ごとの実績）を推奨どおりで承認いただいているため、記録まで廃止すると矛盾する。記録も含めて廃止する意図であれば、F2-6 と `referrers` テーブルもあわせて落とす。

**この決定の帰結**：読み解きガイドのCTAが全員に出るようになる。これは `CLAUDE.md` の「①一般・③④にはガイドCTAを出さない」という現行方針の変更にあたる。ただしF6・F7でオーガニック検索からの一般流入を増やしにいく以上、来た人に何も差し出さない設計はSEO投資と噛み合わないため、整合する方向の変更と考える。実装時に `CLAUDE.md` の `prototype.html` の記述もあわせて更新する。

#### F3-4 診断中の状態

- 設問の途中離脱・リロードは**現行どおり復元しない**（最初からやり直し）。復元を作ると要求4のデバイス束縛と設計が競合するため
- ただし「戻る」（ひとつ前の設問へ）は現行どおり動くこと
- 「もう一度診断する」は**新しい回答として記録**する（既存レコードを上書きしない）。押した時点で古い結果セッションは閉じる

### F4 結果のワンタイム表示とデバイス束縛（要求3後段・要求4）

要求を分解すると3つになる。

- (a) 回答したデバイス・ブラウザ**以外**では結果を見られない
- (b) 同じデバイス・ブラウザでも、**一度画面を閉じたら**もう見られない
- (c) URLを他人に送っても開けない

#### F4-1 「閉じる」の定義

曖昧さを残すと実装できないので、以下を仕様として確定する。

| 操作 | 結果セッション | 理由 |
|:--|:--|:--|
| タブ／ブラウザを閉じる | **終了**（見られなくなる） | 要求そのもの |
| 明示的な「結果を閉じる」ボタン | **終了**（サーバ側で即時無効化） | 意思表示は最優先 |
| 「もう一度診断する」 | **終了**（新しい回答を開始） | |
| ページのリロード（同じタブ） | **継続** | 誤操作で結果が消えると問い合わせが増える（確認事項2で確定） |
| 別のタブで同じURLを開く | **表示しない** | タブを跨がせない |
| 他のアプリへ切り替え／バックグラウンド | **継続** | 画面を閉じていないため。モバイルでの誤爆を避ける |
| 最終操作から30分／発行から2時間 | **終了**（自動失効） | 放置端末からの覗き見対策 |

#### F4-2 実装方式

3つの鍵の**すべてが揃ったときだけ**結果を表示する。

1. **サーバ側の結果セッション**（`result_sessions`）
   - 診断完了時に1件発行。`id`（推測不能なランダム値）・`response_id`・`issued_at`・`expires_at`・`last_seen_at`・`closed_at`・`closed_reason`・`ua_hash`・`ip_hash`
   - 状態は `active` / `closed` / `expired` のいずれか

2. **HttpOnly セッションCookie**（ブラウザ束縛）
   - `Set-Cookie: rs=<token>; HttpOnly; Secure; SameSite=Lax; Path=/`
   - **`Max-Age` と `Expires` を付けない**。これによりブラウザを閉じると消える
   - `HttpOnly` なので JavaScript から読めず、コピーして他デバイスへ移せない → **(a) と (c) を満たす**

3. **sessionStorage の照合値**（タブ束縛）
   - 結果セッション発行時に、Cookieとは別のランダム値を `sessionStorage` に保存し、サーバにも記録する
   - `sessionStorage` は**タブを閉じると確実に消える**。ブラウザ標準の挙動なので取りこぼしがない
   - 表示時に Cookie と sessionStorage の両方をサーバへ渡し、両方一致した場合だけ結果を返す → **(b) を満たし、同一ブラウザの別タブも弾ける**

> `pagehide` / `visibilitychange` で「閉じた」を検知してサーバに通知する方式は採らない。モバイルではアプリ切り替えやスクロールでも発火し、**閉じていないのに結果が消える誤爆**が頻発するため。`sessionStorage` が消えていることを「タブが閉じられた」の正とするのが、最も誤爆が少なく確実。

#### F4-3 URL設計

- 結果のURLは `/result`。**回答IDやトークンをURLに含めない**
- したがってURLをコピーして送っても、受け取った人のブラウザにはCookieもsessionStorageも無いため開けない
- 履歴・ブックマーク・共有シートからも復元できない

#### F4-4 失効後の画面

結果セッションが `closed` / `expired` の状態で `/result` にアクセスされたら、結果は返さず**専用の案内画面**を出す。

- 見出し：この結果はすでに閉じられています
- 本文：診断結果はその場かぎりの表示です。もう一度受けると、あらためて結果をご覧いただけます
- CTA：もう一度診断を受ける（`/` へ）
- HTTPステータスは 410 Gone（noindex）

#### F4-5 読み解きガイドと申込フォームの扱い（確認事項4＝C）

**C を採用する。** 結果画面と読み解きガイド本文はワンタイム（結果セッションで認可）のまま、**体験セッション申込フォームだけを独立URLにして、ワンタイムの外に出す**。

理由：ガイドは8分の読み物で、その終章に申込がある。結果とまとめてワンタイムにすると、途中離脱した人は二度と申込画面に戻れず、成約導線がそのまま取りこぼしになる。申込フォームだけを切り出せば、結果の一回性を損なわずに導線だけ守れる。

| 画面 | 認可 | 閉じたあと |
|:--|:--|:--|
| `/result` 結果 | 結果セッション（3点一致） | 見られない |
| `/guide` ガイド本文 | 結果セッション（3点一致） | 見られない |
| `/apply/{token}` 申込フォーム | **申込トークンのみ** | **開ける** |

##### 申込トークンの仕様

| 項目 | 内容 |
|:--|:--|
| 発行タイミング | 読み解きガイドの**終章を開いた時点**で `apply_tokens` に1件発行する |
| トークン | 推測不能なランダム値（128bit以上）。連番や回答IDそのものは使わない |
| 有効期限 | 発行から **7日間**。期限切れは専用の案内画面（F4-4と同型）を出す |
| 使用回数 | 期限内なら**何度でも開ける**。送信は1回（`used_at` を打ち、以後は「送信済み」表示） |
| 紐づけ | `response_id` を持つため、申込は**自動的に回答へ紐づく**（要求2の要） |
| 開き方 | 終章の申込ボタンから**別タブ**で開く。現行の外部フォームと同じ導線なので、体験は変わらない |
| 案内 | 申込ページ上部に「このページのURLは7日間有効です。あとで申し込む場合はこのタブを残すかブックマークしてください」と明示する |

##### 独立URLにすることの制約（設計上の必須事項）

`/apply/{token}` は URL を知っていれば誰でも開ける。したがって、

- **申込フォームには診断結果を一切表示しない。** タイプ名・3軸・5軸・トリセツ・ヒアリングの入力済み内容を出さない。出すと、URLが人手に渡った時点で結果が漏れ、要求4が破れる
- 表示してよいのは「体験セッションの申込フォームである」ことと、入力欄だけ
- URLが第三者に渡って申込された場合、その申込は**元の回答に紐づいたまま**入る。Adminで氏名を見て判断できるよう、`/admin/sessions` に「同じ回答に2件以上の申込」を検出できる表示を置く
- `noindex, nofollow` と `Cache-Control: no-store` を付ける
- IP単位のレート制限をかける（トークン総当たり対策）

##### 商談前ヒアリングの置き場所

現行はガイド終章（ワンタイム側）にあり、入力のたびに保存している。ここは**ガイド側に残す**。申込フォーム側へ移すと、上記の制約により入力済み内容を再表示できず、書いたものが消えたように見えるため。

#### F4-6 制約として明示すること

- **スクリーンショット・印刷・画面録画は技術的に防げない**。要求の意図が「本人以外に見せない」ではなく「再閲覧・URL共有をさせない」であれば、本設計で満たせる
- プライベートブラウズや `sessionStorage` が使えない環境では結果を表示できない。その場合は理由を明示した案内を出す
- 診断結果はDBに残るため、本人が問い合わせれば運営（Admin）から内容を伝えることはできる

### F5 X共有（要求5）

#### F5-1 配置

結果画面（`/result`）に共有ボタンを置く。押すと X の投稿画面が**別タブ**で開き、本文がプリセットされた状態になる。

#### F5-2 プリセット文面

いただいた文面は次のとおり。

> 「私は{タイプ名」でした！ナチュール診断を受ける`<br>`{URL} #ナチュール診断」

そのままでは投稿できない箇所が2つあるため、**下記のとおり修正する**（確認事項1＝a で確定）。

| 箇所 | 問題 | 修正案 |
|:--|:--|:--|
| `{タイプ名」` | カッコの対応が取れていない（開き `「` がない） | `「{タイプ名}」` |
| `<br>` | X の投稿本文はプレーンテキスト。`<br>` は文字としてそのまま投稿される | 改行（`%0A`）に置き換える |

修正後の文面（`{タイプ名}` に「突撃隊長」などが入る）：

```
私は「{タイプ名}」でした！ナチュール診断を受ける
{URL} #ナチュール診断
```

#### F5-3 URLの仕様

- **`{URL}` は診断のトップページ**（`https://（ドメイン）/`）。結果ページのURLではない
  - 結果はワンタイム・デバイス束縛（F4）のため、共有しても他人は開けない。「診断を受ける」という文面とも整合する
- 流入計測のため `?src=x` を付ける（例 `https://（ドメイン）/?src=x`）
  - **`?ref=` は使わない**。`?ref=` は紹介者コードとしてセグメント判定に使われており、`src=x` からの流入を `referral` と誤判定させないため
  - `?src=x` 付きのURLは `/` へ canonical を向け、インデックスの重複を防ぐ（F6-4）

#### F5-4 遷移先

```
https://x.com/intent/post?text=（本文全体をURLエンコード）
```

- 本文（改行・URL・ハッシュタグを含む全体）を `text` パラメータにまとめて渡す
- `url=` パラメータは**使わない**。使うと X が末尾にURLを付け足し、指定の並び（URLの後にハッシュタグ）が崩れるため
- 文字数：日本語は1文字2カウント、URLは一律23カウント。上記の文面で約90カウントとなり、上限（280）に対して十分な余裕がある

#### F5-5 計測

- 共有ボタンのクリックを記録する（`responses` に `shared_at` / `share_count`、またはイベントテーブル）
- 「見せたい・話したい」という検証アンケートQ3の回答と、実際の共有クリックを突き合わせられるようにする

#### F5-6 OGP（共有時の見え方）

X上でリンクカードが出るよう、トップページに OGP と Twitter Card を設定する（F7-3）。カードが出ないと共有のクリック率が大きく落ちる。

**オプション**：`?src=x&t={タイプコード}` を付け、トップページのOGP画像をタイプ別に出し分ける。タイプ名は投稿本文にすでに出ているので新たな情報漏れはなく、カードの訴求力が上がる。canonical は `/` に固定する。

### F6 検索インデックス設計（要求6）

#### F6-1 前提：現状はインデックスされない

`index.html` は JavaScript の `location.replace` で `prototype.html` へ転送している。これは検索エンジンにとってリダイレクトとして扱われにくく、評価も引き継がれない。さらに `prototype.html` は本文（設問・タイプ）をすべて JavaScript で描画しており、**HTMLソースにはほぼ何も無い**。まずここを直さないと、他のSEO施策は効かない。

#### F6-2 インデックスさせるページ（index）

| ページ | 狙うクエリの例 |
|:--|:--|
| `/` トップ | ナチュール診断／ナチュール診断 無料／人間関係 診断 |
| `/types` 全8タイプ | ナチュール診断 タイプ 一覧／人間関係 タイプ 8 |
| `/types/{code}` 個別8ページ | ナチュール診断 突撃隊長／正論ハンマー 診断／がんばり屋の調整役 |
| `/about` | ナチュール診断とは／ナチュール診断 監修 |
| `/faq` | ナチュール診断 当たる／ナチュール診断 無料／ナチュール診断 登録 |
| 法人LP 3枚 | 業種別の課題クエリ（既存LPの狙いを維持） |

要件：

- **サーバ側でHTMLを生成する**（SSR または SSG）。本文・見出し・リンクがHTMLソースに含まれること
- 1ページ1テーマ。`<h1>` は1つ、見出し階層を正しく積む
- `<title>`・`<meta name="description">`・`<link rel="canonical">` を全ページに設定する
- 内部リンク：トップ → 全8タイプ → 各タイプ → 診断へ戻る、を相互に張る
- パンくず（BreadcrumbList）を設置する
- 画像に `alt`、`width`/`height` を指定する

#### F6-3 インデックスさせないページ（noindex）

| ページ | 方法 |
|:--|:--|
| `/result`・`/guide` | `<meta name="robots" content="noindex, nofollow">` ＋ `X-Robots-Tag: noindex, nofollow` ＋ `Cache-Control: no-store, private` |
| `/admin/**` | 認証必須（未認証は 401/403）＋ 全レスポンスに `X-Robots-Tag: noindex, nofollow` ＋ どこからもリンクしない |
| `/api/**` | `X-Robots-Tag: noindex` |

**注意**：`robots.txt` の `Disallow` だけでは不十分。クロールを禁止すると `noindex` を読ませることすらできず、外部リンクなどからURLだけがインデックスされる場合がある。**Adminは `robots.txt` に頼らず、認証と `X-Robots-Tag` で守る**。`robots.txt` に `/admin/` を書くとパスを公開することにもなるため、書かない方針とする。

`/result` と `/guide` は認証（結果セッション）がないと中身が出ないため、`noindex` ヘッダで十分。

#### F6-4 クロール制御

- `robots.txt`：`/api/` を Disallow、`Sitemap:` で sitemap.xml の場所を示す
- `sitemap.xml`：index対象のページのみを自動生成する。`/result`・`/guide`・`/admin` は含めない
- クエリ付きURL（`?src=x`・`?ref=`・`?seg=`・`?mode=`）はすべて `/` へ canonical を向ける。パラメータ違いの重複インデックスを防ぐ
- 旧URLは301：`/prototype.html` → `/`、`/all-types.html` → `/types`、`/index.html` → `/`

### F7 「ナチュール診断」指名検索対策（要求7）

#### F7-1 前提の整理

指名検索（ブランド名での検索）は、**まず名前が知られていないと発生しない**。SEOで検索需要そのものを作ることはできない。したがって役割を2つに分ける。

| 役割 | 手段 |
|:--|:--|
| 検索需要をつくる（オフサイト） | X共有（F5）、紹介、法人LP、登壇、note などの外部発信 |
| 検索されたときに必ず1位で受け止める（オンサイト） | 本節のSEO対策 |

本書が扱うのは後者。前者は `0to1マーケティング戦略.md` の管轄。

#### F7-2 事前調査（実装前に必ず行う）

- **同名・類似名の競合調査**：「ナチュール」はフランス語由来で、美容室・化粧品・食品ブランドに多用されている語。「ナチュール診断」という複合語での競合状況、および「ナチュール」単体での上位を確認する
- 想定クエリのボリュームと現在の順位を記録し、施策の前後で比較できるようにする
- 商標の確認（同名サービスとの衝突リスク）

#### F7-3 オンサイトの必須項目

**サイト名の統一**。以下すべてで表記を「ナチュール診断」に統一する。表記が割れるとブランドとして認識されない。

- `<title>` の末尾（例：`人間関係タイプ診断 | ナチュール診断`）
- OGP の `og:site_name`
- 構造化データ `WebSite.name`
- ファビコン・ヘッダーの表示名
- 法人LP・全8タイプページを含む全ページ

**メタ情報**

| 項目 | 内容 |
|:--|:--|
| `<title>` | 30文字前後。トップは「ナチュール診断」を必ず含める |
| `description` | 90〜120文字。「登録不要」「無料」「2分」「8タイプ」など、クリックの理由になる語を入れる |
| `canonical` | 全ページ |
| `og:title` / `og:description` / `og:image` / `og:url` / `og:type` / `og:site_name` / `og:locale`（`ja_JP`） | 全ページ |
| `twitter:card` | `summary_large_image` |
| OGP画像 | 1200×630px。診断名が読める大きさで入っていること |
| `<html lang="ja">` | 全ページ |

**構造化データ（JSON-LD）**

| スキーマ | 対象ページ | 狙い |
|:--|:--|:--|
| `WebSite`（`name`・`url`・`alternateName`） | 全ページ | サイト名としての認識。`alternateName` に表記ゆれを入れる |
| `Organization` または `Person`（提供者） | トップ・`/about` | 提供元の明示（E-E-A-T） |
| `BreadcrumbList` | 下層ページ | パンくず表示 |
| `FAQPage` | `/faq` | リッチリザルト・AI要約での引用 |
| `Article` | `/types/{code}` | タイプ個別ページの評価 |

**「ナチュール診断とは」への一問一答**

トップと `/about` の冒頭に、**1〜2文で完結する定義文**を置く。検索結果のスニペットとAI要約に引用されやすい形にする。

> ナチュール診断は、9つの質問から「自然体のあなた」の人間関係の関わり方を8タイプで映し出す無料の診断です。登録は不要で、2分で受けられます。

**表記ゆれの吸収**

「ナチュール診断」「なちゅーる診断」「Nature診断」「ナチュール しんだん」などの表記を、FAQ本文や `alternateName` に自然に含める。不自然な羅列はしない。

**二次クエリを受けるページ**

指名検索のあとに続く検索（「ナチュール診断 当たる」「ナチュール診断 無料」「ナチュール診断 タイプ 一覧」「ナチュール診断 結果」）を、FAQと全8タイプページで受け止める。

**タイプ個別ページ（8枚）でロングテールを面で取る**

各タイプの名前（突撃隊長／正論ハンマー／お祭り隊長／自由人コメンテーター／沈黙の大黒柱／縁の下の職人／根回しの仕掛け人／がんばり屋の調整役）は固有名詞性が高く、競合が少ない。1タイプ1ページで、あるある・強み・トリセツ・相性・その他タイプへのリンク・診断への導線を持たせる。

**注意**：結果画面で本人に見せる内容と完全に同一にすると、独自性が薄れる。タイプ個別ページは「読み物」として編集し直す。

#### F7-4 技術的な土台

| 項目 | 要件 |
|:--|:--|
| 独自ドメイン | `*.github.io` のサブドメインではブランド想起が弱く、共有時の見え方も劣る。**独自ドメインを取得する**（確認事項5で確定） |
| HTTPS | 必須 |
| モバイル対応 | 診断利用の大半はモバイル想定。モバイルファーストで検証する |
| Core Web Vitals | LCP 2.5秒以下、CLS 0.1以下、INP 200ms以下 |
| フォント | Noto Sans JP / Noto Serif JP は日本語で重い。**サブセット化とセルフホスト**を検討し、`font-display: swap` を維持。LCPへの影響が最も大きい箇所 |
| Google Search Console | 登録・sitemap送信・インデックス登録リクエスト・カバレッジ監視 |
| Bing Webmaster Tools | 登録（Copilot などのAI検索の参照元になる） |
| アナリティクス | 流入クエリと診断完了率を突き合わせられること |

#### F7-5 オフサイト（サイテーション）

- X の公式アカウントを作り、プロフィールにURLを置く
- 法人LP 3枚から診断へリンクする（すでに導線あり。`rel="noopener"` は維持しつつ `nofollow` は付けない）
- note・登壇資料・紹介キットなど、既存の発信物から一貫した名前とURLでリンクする

## 6. 非機能要件

### 6.1 セキュリティ

| 項目 | 要件 |
|:--|:--|
| 通信 | 全ページ HTTPS。HSTS を有効化 |
| Admin認証 | パスワードハッシュ化、試行回数制限、セッションタイムアウト30分 |
| Cookie | HttpOnly / Secure / SameSite。結果セッションCookieは `Max-Age` なし |
| CSRF | 状態を変えるAPIにCSRFトークン、または SameSite=Strict＋Originチェック |
| 入力検証 | 全APIでサーバ側バリデーション。タイプ判定はサーバ側で再計算 |
| レート制限 | 回答送信・申込送信・ログインにIP単位の制限 |
| CSP | `Content-Security-Policy` を設定。インラインスクリプトは nonce 管理 |
| シークレット | 環境変数で管理。リポジトリにコミットしない（現状 Apps Script URL・フォームURLはHTMLに直書き） |
| 依存関係 | 脆弱性スキャンをCIに入れる |

### 6.2 個人情報・プライバシー

| 項目 | 要件 |
|:--|:--|
| 取得する個人情報 | 体験セッション申込の氏名・メールアドレス、法人リードのメールアドレス、ヒアリング本文 |
| 診断回答の扱い | 単体では個人を特定しないが、申込と紐づくと個人情報になる。**紐づく前提で扱う** |
| プライバシーポリシー | `/privacy` を新設。取得項目・利用目的・保存期間・第三者提供の有無・開示/削除の窓口を明記 |
| 利用規約 | `/terms` を新設 |
| 同意 | 申込フォーム送信時にプライバシーポリシーへの同意を取る |
| IPアドレス | 生値は保存しない。ソルト付きハッシュのみ |
| 保存期間 | **回答データは3年**（確認事項7）。回答日から3年を過ぎたレコードを自動削除するバッチを持ち、`responses.purge_after` を基準日にする。申込・法人リードなど取引関係のある個人情報は、必要なら別途期間を定める |
| 削除依頼 | 回答IDまたはメールアドレスから該当データを削除できる導線をAdminに用意（論理削除＋物理削除） |
| Cookie | 結果表示のためのセッションCookieは機能上必須のため同意不要。解析Cookie（GA4等）を入れる場合は同意バナーを検討 |
| Admin側の配慮 | 氏名・メールは一覧では一部マスク表示し、詳細でのみ全表示。閲覧を監査ログに残す |
| 保存期間の運用 | 自動削除は**実行前に件数をログに残し**、誤って全件消さないようドライラン可能にする。削除済みの件数はAdminから確認できること |

### 6.3 性能・可用性

- 想定同時アクセス：ピークでも数十件/分（紹介・X共有からのスパイクを想定）
- 結果表示のレスポンス：p95で1秒以内
- 可用性：99.5%程度で十分（マネージドサービスの標準SLAに委ねる）
- バックアップ：DBの日次自動バックアップ、7日以上の保持、復旧手順の文書化
- エラー監視：サーバエラー・API失敗率のアラート

### 6.4 計測

DBに記録するファネルの各段（Adminまたは解析ツールで確認できること）：

`トップ表示 → 診断開始 → 設問完了 → 結果表示 → X共有クリック → ガイドを開く → 終章到達 → 体験セッション申込`

セグメント（`general` / `referral` / `leader-lp` / `member`）別、紹介者別、流入元別に見られること。

### 6.5 運用・保守

- 設問・タイプ文面・ガイド本文の変更手順を文書化する（どのファイルを直せば反映されるか）
- 設問を変えるときは `question_set_version` を上げる。過去データの解釈を壊さない
- ステージング環境を持ち、本番へ反映する前に確認できること

## 7. データ移行

### 7.1 移行対象

| 移行元 | 移行先 | 備考 |
|:--|:--|:--|
| `指標v2` シート | `responses` ＋ `feedback_surveys` ＋ `hearings` | 9問の回答は `response_answers` に展開。15問分は移行元に無いため欠損として扱う |
| `法人リード` シート | `corp_leads` | 「課題」の「、」連結を配列に分解 |
| `紹介者マスタ` シート | `referrers` | |
| Googleフォーム回答シート | `session_applications` | 「診断ID」がある行は `response_id` に紐づけ、無い行は未紐づけで取り込み、Adminから手動紐づけ。移行後、**Googleフォームは受付を締め切る**（確認事項3＝a） |
| `ガイド文面` / `ガイド設定` シート | 移行しない | メール配信は停止中。スプレッドシートに温存 |

### 7.2 移行の進め方

1. スプレッドシートをCSVで書き出し、変換スクリプトでDBへ投入する
2. 件数・タイプ別分布・日付範囲を移行前後で照合する
3. 移行後もスプレッドシートは**読み取り専用**で残す（削除しない）
4. Apps Script Web App は、新アプリの公開後**一定期間は稼働させたまま**にする（旧URLが拡散済みの可能性があるため）。旧ページを新ドメインへリダイレクトし、流入がゼロになったら停止する

### 7.3 移行時の注意

- 移行元には**15問のリッカート生値が存在しない**。移行データと新規データで分析の粒度が変わることを、Admin上で判別できるようにする（`question_set_version` に `legacy` を入れる）
- 「紹介者名」列は VLOOKUP の計算結果。値として書き出してから移行する
- タイムスタンプのタイムゾーン（JST）を明示して変換する
- 移行データの `mode` と `segment` はそのまま保存する。新規収集は廃止したが（確認事項9＝b）、過去のセグメント別の傾向を後から見られるようにするため
- **保存期間3年（6.2）を移行データにも適用する。** 移行時点で回答日から3年を過ぎている行があれば、投入前に扱いを決める（移行しない、または移行して次回のバッチで削除する）

## 8. 技術構成の提案

要件を満たす最小構成として、以下を推奨する。実装者の得意分野で置き換えて構わないが、**SSR・DB・httpOnly Cookie・認証**の4点は必須。

| 層 | 推奨 | 理由 |
|:--|:--|:--|
| フレームワーク | Next.js（App Router） | SSR/SSGとAPIを1つのコードベースで持てる。F6のSEO要件と相性が良い |
| ホスティング | Vercel など | 独自ドメイン・HTTPS・リダイレクト・ヘッダー設定が標準機能 |
| データベース | マネージド PostgreSQL（Neon / Supabase など） | 配列型・JSONB・全文検索が使え、バックアップが標準 |
| ORM | Prisma | スキーマとマイグレーションの管理 |
| 認証（Admin） | 自前セッション or Auth.js | 利用者が少数のため軽量で足りる |
| 解析 | GA4 ＋ Search Console | 標準的な組み合わせ |

**代替案**：Cloudflare Workers ＋ D1（SQLite）でも要件は満たせる。コストは低いが、SSRの書き味とエコシステムでは Next.js が有利。

**採らない案**：現行の静的HTML＋Apps Script の延長。F4（サーバ側の結果認可）とF2（Admin）とF6（SSR）のいずれも満たせない。

## 9. リリース計画

| Phase | 内容 | 完了の定義 |
|:--|:--|:--|
| **0. 準備** | 独自ドメイン取得、「ナチュール」の同名・商標調査（F7-2）、環境構築、プライバシーポリシーと利用規約の文面確定 | 着手できる状態 |
| **1. 基盤と回答画面** | DBスキーマ、回答画面の移植（F3）、ワンタイム結果（F4）、**申込フォーム `/apply/{token}`**、X共有（F5）、SSRの土台とメタ情報 | 新アプリで診断から申込まで通しで完了し、DBに記録される |
| **2. Admin とデータ移行** | Admin全画面（F2）、既存4か所からのデータ移行（第7章）、Googleフォームの受付停止 | スプレッドシートを開かずに運用が回る |
| **3. SEO本格化** | タイプ個別8ページ、FAQ、about、構造化データ、sitemap、Search Console登録、旧URLの301（F6・F7） | 「ナチュール診断」で自サイトが1位 |
| **4. 拡張** | 法人LP 3枚の移設、保存期間3年の自動削除バッチ、A/Bテスト基盤 | |

**v1 から変えた点**：v1 では申込フォームの内製化を Phase 4 に置いていたが、**Phase 1 へ前倒しした**。確認事項4＝C により申込フォームが結果セッションの認可設計（F4）の一部になったこと、および確認事項3＝a で Googleフォームを廃止すると決まったことによる。Googleフォームを一度アプリに繋ぎ込んでから作り直す二度手間も避けられる。確認事項10＝b により、集計ダッシュボードは計画から外した。

Phase 1 と 2 の間で一度本番投入し、旧ページからリダイレクトする。Phase 3 以降は運用しながら進める。

## 10. 決定記録（確認事項10件・2026-09-02 確定）

| # | 論点 | 決定 | 反映先 |
|:--|:--|:--|:--|
| 1 | X共有の文面 | **修正案どおり**。`「{タイプ名}」`＋改行にする | F5-2 |
| 2 | 結果画面のリロード | **リロードでは消さない** | F4-1 |
| 3 | 体験セッション申込フォーム | **アプリ内に内製化**。Googleフォームは廃止 | F4-5・第7章・Phase 1〜2 |
| 4 | ガイドと申込のワンタイム適用 | **C：申込フォームだけ独立URL**。結果とガイド本文はワンタイムのまま | F4-5 |
| 5 | ドメイン | **独自ドメインを取得する** | F7-4・Phase 0 |
| 6 | 法人LPと全8タイプページ | **全8タイプはアプリに取り込む**（Phase 3）。**法人LPは Phase 4 で移設** | F6-2・Phase 3〜4 |
| 7 | 回答データの保存期間 | **3年**。自動削除バッチを持つ | 6.2・7.3・Phase 4 |
| 8 | Adminの利用者 | **1名・権限分離なし**。`role` は持たない | F1-2・F2-1 |
| 9 | 検証モードとセグメント出し分け | **廃止**。全員に同じ画面を出す。`?ref=` は記録のみ残す | F3-3 |
| 10 | 集計ダッシュボード | **作らない**。CSVエクスポート＋スプレッドシートで足りる | 第9章・第11章 |

### 決定にともなって残る注意点

1. **確認事項9の「廃止」の範囲**（F3-3）。画面の出し分けの廃止と読み、`?ref=` による流入元の記録は残した。記録も含めて廃止する意図であれば、F2-6（紹介者マスタ）と `referrers` テーブルもあわせて落とす必要がある。
2. **ガイドCTAが全員に出るようになる**（F3-3）。`CLAUDE.md` の「①一般・③④にはガイドCTAを出さない」という現行方針の変更にあたるため、実装時に `CLAUDE.md` も更新する。
3. **申込フォームには診断結果を表示できない**（F4-5）。独立URLは誰でも開けるため、タイプ名を含めて結果を出すと要求4が破れる。
4. **1名運用ゆえの単一障害点**（F2-1）。権限分離は不要でも、ログイン通知とパスワード復旧手段は省略しない。

## 11. スコープ外・将来検討

- 回答者向けのマイページ・診断履歴（要求4と矛盾するため作らない）
- 相性診断（2人のタイプを突き合わせる機能）。`相性ルール.md` に素材あり
- チームレポート（複数メンバーの結果を束ねる）。`法人中心価値とファネル.md` では**M3の自動化が年5,000万円の前提条件**とされており、事業上の優先度は高い。アプリ化の設計時に、responses を「チーム」で束ねられる余地（`team_id`）を残しておく
- 読み解きガイドのメール配信の再開（`apps-script/Guide.gs` は温存）
- **集計ダッシュボード**（確認事項10＝b で当面は作らない）。CSVエクスポートを回す手間が増えたら再検討する
- **検証アンケートの再開**（確認事項9＝b で廃止）。設問を変えたあとに再検証したくなった場合に備え、`feedback_surveys` テーブルと過去データは残す
- **Adminの二要素認証と権限分離**（確認事項8で当面は不要）。増員時に `admin_users.role` を追加する
- A/Bテスト基盤（`競合ティアダウン_ABテスト案.md`）
- 多言語対応

## 変更履歴

| 日付 | 版 | 内容 |
|:--|:--|:--|
| 2026-09-02 | v1 | 初版。要求7項目を要件F1〜F7に整理 |
| 2026-09-02 | v2 | 確認事項10件を確定して全体へ反映。主な変更：申込フォームを独立URL `/apply/{token}` として内製化（4＝C・3＝a）、セグメント出し分けと検証アンケートを廃止（9＝b）、保存期間を3年に確定（7）、Adminを1名運用に（8）、ダッシュボードをスコープ外へ（10＝b）、申込フォームの実装を Phase 1 へ前倒し |
